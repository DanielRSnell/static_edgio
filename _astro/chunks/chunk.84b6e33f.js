/* empty css                */import { c as createAstro, a as createComponent, r as renderTemplate, e as renderComponent, m as maybeRenderHead, F as Fragment, u as unescapeHTML } from './chunk.8ece003f.js';
import { $ as $$Frame } from './chunk.7dbb871e.js';
import * as cheerio from 'cheerio';

const $$Astro$6S = createAstro();
const $$Index$6S = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6S, $$props, $$slots);
  Astro2.self = $$Index$6S;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/index.astro");

const $$file$6S = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/index.astro";
const $$url$6S = "";

const index$6S = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6S,
   file: $$file$6S,
   url: $$url$6S
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6R = createAstro();
const $$Index$6R = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6R, $$props, $$slots);
  Astro2.self = $$Index$6R;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/security-compliance/index.astro");

const $$file$6R = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/security-compliance/index.astro";
const $$url$6R = "/security-compliance";

const index$6R = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6R,
   file: $$file$6R,
   url: $$url$6R
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6Q = createAstro();
const $$Index$6Q = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6Q, $$props, $$slots);
  Astro2.self = $$Index$6Q;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/talk-to-an-expert/index.astro");

const $$file$6Q = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/talk-to-an-expert/index.astro";
const $$url$6Q = "/talk-to-an-expert";

const index$6Q = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6Q,
   file: $$file$6Q,
   url: $$url$6Q
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6P = createAstro();
const $$Index$6P = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6P, $$props, $$slots);
  Astro2.self = $$Index$6P;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/contact-support/index.astro");

const $$file$6P = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/contact-support/index.astro";
const $$url$6P = "/contact-support";

const index$6P = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6P,
   file: $$file$6P,
   url: $$url$6P
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6O = createAstro();
const $$Index$6O = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6O, $$props, $$slots);
  Astro2.self = $$Index$6O;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/contact-us/index.astro");

const $$file$6O = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/contact-us/index.astro";
const $$url$6O = "/contact-us";

const index$6O = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6O,
   file: $$file$6O,
   url: $$url$6O
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6N = createAstro();
const $$Index$6N = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6N, $$props, $$slots);
  Astro2.self = $$Index$6N;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/index.astro");

const $$file$6N = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/index.astro";
const $$url$6N = "/resources";

const index$6N = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6N,
   file: $$file$6N,
   url: $$url$6N
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6M = createAstro();
const $$Index$6M = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6M, $$props, $$slots);
  Astro2.self = $$Index$6M;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-articles/index.astro");

const $$file$6M = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-articles/index.astro";
const $$url$6M = "/resources/technical-articles";

const index$6M = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6M,
   file: $$file$6M,
   url: $$url$6M
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6L = createAstro();
const $$Index$6L = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6L, $$props, $$slots);
  Astro2.self = $$Index$6L;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/implementing-edgio-delivery-cdn-as-an-ingress-provider-in-your-kubernetes-cluster/index.astro");

const $$file$6L = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/implementing-edgio-delivery-cdn-as-an-ingress-provider-in-your-kubernetes-cluster/index.astro";
const $$url$6L = "/resources/technical-article/implementing-edgio-delivery-cdn-as-an-ingress-provider-in-your-kubernetes-cluster";

const index$6L = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6L,
   file: $$file$6L,
   url: $$url$6L
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6K = createAstro();
const $$Index$6K = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6K, $$props, $$slots);
  Astro2.self = $$Index$6K;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/improving-network-performance-with-dynamic-congestion-control/index.astro");

const $$file$6K = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/improving-network-performance-with-dynamic-congestion-control/index.astro";
const $$url$6K = "/resources/technical-article/improving-network-performance-with-dynamic-congestion-control";

const index$6K = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6K,
   file: $$file$6K,
   url: $$url$6K
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6J = createAstro();
const $$Index$6J = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6J, $$props, $$slots);
  Astro2.self = $$Index$6J;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/measuring-video-qoe-from-the-server-side-estimate-rebuffering/index.astro");

const $$file$6J = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/measuring-video-qoe-from-the-server-side-estimate-rebuffering/index.astro";
const $$url$6J = "/resources/technical-article/measuring-video-qoe-from-the-server-side-estimate-rebuffering";

const index$6J = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6J,
   file: $$file$6J,
   url: $$url$6J
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6I = createAstro();
const $$Index$6I = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6I, $$props, $$slots);
  Astro2.self = $$Index$6I;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/video-encoding-in-the-cloud-to-stream-large-scale-live-events/index.astro");

const $$file$6I = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/video-encoding-in-the-cloud-to-stream-large-scale-live-events/index.astro";
const $$url$6I = "/resources/technical-article/video-encoding-in-the-cloud-to-stream-large-scale-live-events";

const index$6I = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6I,
   file: $$file$6I,
   url: $$url$6I
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6H = createAstro();
const $$Index$6H = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6H, $$props, $$slots);
  Astro2.self = $$Index$6H;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/best-practices-for-deploying-drm-in-large-scale-ott-stack/index.astro");

const $$file$6H = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/best-practices-for-deploying-drm-in-large-scale-ott-stack/index.astro";
const $$url$6H = "/resources/technical-article/best-practices-for-deploying-drm-in-large-scale-ott-stack";

const index$6H = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6H,
   file: $$file$6H,
   url: $$url$6H
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6G = createAstro();
const $$Index$6G = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6G, $$props, $$slots);
  Astro2.self = $$Index$6G;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/evaluating-an-adaptive-load-balancing-system-for-our-cdn/index.astro");

const $$file$6G = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/evaluating-an-adaptive-load-balancing-system-for-our-cdn/index.astro";
const $$url$6G = "/resources/technical-article/evaluating-an-adaptive-load-balancing-system-for-our-cdn";

const index$6G = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6G,
   file: $$file$6G,
   url: $$url$6G
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6F = createAstro();
const $$Index$6F = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6F, $$props, $$slots);
  Astro2.self = $$Index$6F;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/why-edge-applications-are-the-key-to-simplifying-edge/index.astro");

const $$file$6F = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/why-edge-applications-are-the-key-to-simplifying-edge/index.astro";
const $$url$6F = "/resources/technical-article/why-edge-applications-are-the-key-to-simplifying-edge";

const index$6F = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6F,
   file: $$file$6F,
   url: $$url$6F
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6E = createAstro();
const $$Index$6E = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6E, $$props, $$slots);
  Astro2.self = $$Index$6E;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/large-scale-socket-performance-monitoring-with-xtcp/index.astro");

const $$file$6E = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/large-scale-socket-performance-monitoring-with-xtcp/index.astro";
const $$url$6E = "/resources/technical-article/large-scale-socket-performance-monitoring-with-xtcp";

const index$6E = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6E,
   file: $$file$6E,
   url: $$url$6E
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6D = createAstro();
const $$Index$6D = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6D, $$props, $$slots);
  Astro2.self = $$Index$6D;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/using-rtt-metrics-to-improve-streaming-performance/index.astro");

const $$file$6D = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/using-rtt-metrics-to-improve-streaming-performance/index.astro";
const $$url$6D = "/resources/technical-article/using-rtt-metrics-to-improve-streaming-performance";

const index$6D = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6D,
   file: $$file$6D,
   url: $$url$6D
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6C = createAstro();
const $$Index$6C = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6C, $$props, $$slots);
  Astro2.self = $$Index$6C;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/better-transparency-and-troubleshooting-for-ssai/index.astro");

const $$file$6C = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/better-transparency-and-troubleshooting-for-ssai/index.astro";
const $$url$6C = "/resources/technical-article/better-transparency-and-troubleshooting-for-ssai";

const index$6C = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6C,
   file: $$file$6C,
   url: $$url$6C
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6B = createAstro();
const $$Index$6B = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6B, $$props, $$slots);
  Astro2.self = $$Index$6B;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/optimize-session-drift-in-live-streams-with-ssai/index.astro");

const $$file$6B = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/optimize-session-drift-in-live-streams-with-ssai/index.astro";
const $$url$6B = "/resources/technical-article/optimize-session-drift-in-live-streams-with-ssai";

const index$6B = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6B,
   file: $$file$6B,
   url: $$url$6B
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6A = createAstro();
const $$Index$6A = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6A, $$props, $$slots);
  Astro2.self = $$Index$6A;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/stonefish-automating-ddos-mitigation-at-the-edge/index.astro");

const $$file$6A = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/stonefish-automating-ddos-mitigation-at-the-edge/index.astro";
const $$url$6A = "/resources/technical-article/stonefish-automating-ddos-mitigation-at-the-edge";

const index$6A = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6A,
   file: $$file$6A,
   url: $$url$6A
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6z = createAstro();
const $$Index$6z = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6z, $$props, $$slots);
  Astro2.self = $$Index$6z;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/get-personal-with-millions-by-scaling-manifest/index.astro");

const $$file$6z = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/get-personal-with-millions-by-scaling-manifest/index.astro";
const $$url$6z = "/resources/technical-article/get-personal-with-millions-by-scaling-manifest";

const index$6z = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6z,
   file: $$file$6z,
   url: $$url$6z
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6y = createAstro();
const $$Index$6y = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6y, $$props, $$slots);
  Astro2.self = $$Index$6y;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/configuring-the-edge-for-your-online-business/index.astro");

const $$file$6y = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/configuring-the-edge-for-your-online-business/index.astro";
const $$url$6y = "/resources/technical-article/configuring-the-edge-for-your-online-business";

const index$6y = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6y,
   file: $$file$6y,
   url: $$url$6y
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6x = createAstro();
const $$Index$6x = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6x, $$props, $$slots);
  Astro2.self = $$Index$6x;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/low-latency-live-streaming-with-a-faster-cdn/index.astro");

const $$file$6x = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/low-latency-live-streaming-with-a-faster-cdn/index.astro";
const $$url$6x = "/resources/technical-article/low-latency-live-streaming-with-a-faster-cdn";

const index$6x = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6x,
   file: $$file$6x,
   url: $$url$6x
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6w = createAstro();
const $$Index$6w = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6w, $$props, $$slots);
  Astro2.self = $$Index$6w;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/caching-strategies-for-better-streaming/index.astro");

const $$file$6w = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/caching-strategies-for-better-streaming/index.astro";
const $$url$6w = "/resources/technical-article/caching-strategies-for-better-streaming";

const index$6w = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6w,
   file: $$file$6w,
   url: $$url$6w
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6v = createAstro();
const $$Index$6v = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6v, $$props, $$slots);
  Astro2.self = $$Index$6v;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/can-you-benefit-from-server-automation/index.astro");

const $$file$6v = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/can-you-benefit-from-server-automation/index.astro";
const $$url$6v = "/resources/technical-article/can-you-benefit-from-server-automation";

const index$6v = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6v,
   file: $$file$6v,
   url: $$url$6v
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6u = createAstro();
const $$Index$6u = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6u, $$props, $$slots);
  Astro2.self = $$Index$6u;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/running-a-multi-tenant-waf-at-the-edge/index.astro");

const $$file$6u = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/running-a-multi-tenant-waf-at-the-edge/index.astro";
const $$url$6u = "/resources/technical-article/running-a-multi-tenant-waf-at-the-edge";

const index$6u = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6u,
   file: $$file$6u,
   url: $$url$6u
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6t = createAstro();
const $$Index$6t = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6t, $$props, $$slots);
  Astro2.self = $$Index$6t;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/optimizing-the-cdn-for-live-streaming/index.astro");

const $$file$6t = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/optimizing-the-cdn-for-live-streaming/index.astro";
const $$url$6t = "/resources/technical-article/optimizing-the-cdn-for-live-streaming";

const index$6t = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6t,
   file: $$file$6t,
   url: $$url$6t
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6s = createAstro();
const $$Index$6s = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6s, $$props, $$slots);
  Astro2.self = $$Index$6s;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/detecting-waves-with-shakealert/index.astro");

const $$file$6s = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/detecting-waves-with-shakealert/index.astro";
const $$url$6s = "/resources/technical-article/detecting-waves-with-shakealert";

const index$6s = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6s,
   file: $$file$6s,
   url: $$url$6s
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6r = createAstro();
const $$Index$6r = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6r, $$props, $$slots);
  Astro2.self = $$Index$6r;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/bbr-evaluation-at-a-large-cdn/index.astro");

const $$file$6r = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/bbr-evaluation-at-a-large-cdn/index.astro";
const $$url$6r = "/resources/technical-article/bbr-evaluation-at-a-large-cdn";

const index$6r = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6r,
   file: $$file$6r,
   url: $$url$6r
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6q = createAstro();
const $$Index$6q = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6q, $$props, $$slots);
  Astro2.self = $$Index$6q;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/server-optimization-at-scale/index.astro");

const $$file$6q = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/technical-article/server-optimization-at-scale/index.astro";
const $$url$6q = "/resources/technical-article/server-optimization-at-scale";

const index$6q = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6q,
   file: $$file$6q,
   url: $$url$6q
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6p = createAstro();
const $$Index$6p = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6p, $$props, $$slots);
  Astro2.self = $$Index$6p;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/analyst-report/edgio-included-in-new-idc-report-for-cloud-platform-capabilities/index.astro");

const $$file$6p = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/analyst-report/edgio-included-in-new-idc-report-for-cloud-platform-capabilities/index.astro";
const $$url$6p = "/resources/analyst-report/edgio-included-in-new-idc-report-for-cloud-platform-capabilities";

const index$6p = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6p,
   file: $$file$6p,
   url: $$url$6p
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6o = createAstro();
const $$Index$6o = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6o, $$props, $$slots);
  Astro2.self = $$Index$6o;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/analyst-report/edgio-leader-in-spark-matrix-quadrant-report/index.astro");

const $$file$6o = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/analyst-report/edgio-leader-in-spark-matrix-quadrant-report/index.astro";
const $$url$6o = "/resources/analyst-report/edgio-leader-in-spark-matrix-quadrant-report";

const index$6o = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6o,
   file: $$file$6o,
   url: $$url$6o
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6n = createAstro();
const $$Index$6n = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6n, $$props, $$slots);
  Astro2.self = $$Index$6n;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/index.astro");

const $$file$6n = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/index.astro";
const $$url$6n = "/resources/client-stories";

const index$6n = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6n,
   file: $$file$6n,
   url: $$url$6n
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6m = createAstro();
const $$Index$6m = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6m, $$props, $$slots);
  Astro2.self = $$Index$6m;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/theme-based-channels-maximize-user-acquisition-engage-viewers-longer/index.astro");

const $$file$6m = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/theme-based-channels-maximize-user-acquisition-engage-viewers-longer/index.astro";
const $$url$6m = "/resources/client-stories/theme-based-channels-maximize-user-acquisition-engage-viewers-longer";

const index$6m = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6m,
   file: $$file$6m,
   url: $$url$6m
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6l = createAstro();
const $$Index$6l = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6l, $$props, $$slots);
  Astro2.self = $$Index$6l;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/washington-state-bar-association/index.astro");

const $$file$6l = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/washington-state-bar-association/index.astro";
const $$url$6l = "/resources/client-stories/washington-state-bar-association";

const index$6l = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6l,
   file: $$file$6l,
   url: $$url$6l
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6k = createAstro();
const $$Index$6k = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6k, $$props, $$slots);
  Astro2.self = $$Index$6k;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/premier-league-football-club/index.astro");

const $$file$6k = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/premier-league-football-club/index.astro";
const $$url$6k = "/resources/client-stories/premier-league-football-club";

const index$6k = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6k,
   file: $$file$6k,
   url: $$url$6k
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6j = createAstro();
const $$Index$6j = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6j, $$props, $$slots);
  Astro2.self = $$Index$6j;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/financial-business-systems/index.astro");

const $$file$6j = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/financial-business-systems/index.astro";
const $$url$6j = "/resources/client-stories/financial-business-systems";

const index$6j = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6j,
   file: $$file$6j,
   url: $$url$6j
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6i = createAstro();
const $$Index$6i = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6i, $$props, $$slots);
  Astro2.self = $$Index$6i;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/world-champion-fantasy/index.astro");

const $$file$6i = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/world-champion-fantasy/index.astro";
const $$url$6i = "/resources/client-stories/world-champion-fantasy";

const index$6i = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6i,
   file: $$file$6i,
   url: $$url$6i
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6h = createAstro();
const $$Index$6h = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6h, $$props, $$slots);
  Astro2.self = $$Index$6h;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/arsenal-football-club/index.astro");

const $$file$6h = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/arsenal-football-club/index.astro";
const $$url$6h = "/resources/client-stories/arsenal-football-club";

const index$6h = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6h,
   file: $$file$6h,
   url: $$url$6h
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6g = createAstro();
const $$Index$6g = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6g, $$props, $$slots);
  Astro2.self = $$Index$6g;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/mitsubachiworks-inc/index.astro");

const $$file$6g = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/mitsubachiworks-inc/index.astro";
const $$url$6g = "/resources/client-stories/mitsubachiworks-inc";

const index$6g = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6g,
   file: $$file$6g,
   url: $$url$6g
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6f = createAstro();
const $$Index$6f = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6f, $$props, $$slots);
  Astro2.self = $$Index$6f;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/universal-standard/index.astro");

const $$file$6f = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/universal-standard/index.astro";
const $$url$6f = "/resources/client-stories/universal-standard";

const index$6f = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6f,
   file: $$file$6f,
   url: $$url$6f
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6e = createAstro();
const $$Index$6e = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6e, $$props, $$slots);
  Astro2.self = $$Index$6e;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/washington-and-lee/index.astro");

const $$file$6e = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/washington-and-lee/index.astro";
const $$url$6e = "/resources/client-stories/washington-and-lee";

const index$6e = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6e,
   file: $$file$6e,
   url: $$url$6e
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6d = createAstro();
const $$Index$6d = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6d, $$props, $$slots);
  Astro2.self = $$Index$6d;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/authentic-gaming/index.astro");

const $$file$6d = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/authentic-gaming/index.astro";
const $$url$6d = "/resources/client-stories/authentic-gaming";

const index$6d = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6d,
   file: $$file$6d,
   url: $$url$6d
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6c = createAstro();
const $$Index$6c = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6c, $$props, $$slots);
  Astro2.self = $$Index$6c;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/nintendo-europe/index.astro");

const $$file$6c = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/nintendo-europe/index.astro";
const $$url$6c = "/resources/client-stories/nintendo-europe";

const index$6c = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6c,
   file: $$file$6c,
   url: $$url$6c
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6b = createAstro();
const $$Index$6b = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6b, $$props, $$slots);
  Astro2.self = $$Index$6b;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/cogeco-peer-1/index.astro");

const $$file$6b = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/cogeco-peer-1/index.astro";
const $$url$6b = "/resources/client-stories/cogeco-peer-1";

const index$6b = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6b,
   file: $$file$6b,
   url: $$url$6b
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6a = createAstro();
const $$Index$6a = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6a, $$props, $$slots);
  Astro2.self = $$Index$6a;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/learning-ally/index.astro");

const $$file$6a = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/learning-ally/index.astro";
const $$url$6a = "/resources/client-stories/learning-ally";

const index$6a = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6a,
   file: $$file$6a,
   url: $$url$6a
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$69 = createAstro();
const $$Index$69 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$69, $$props, $$slots);
  Astro2.self = $$Index$69;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/shoe-carnival/index.astro");

const $$file$69 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/shoe-carnival/index.astro";
const $$url$69 = "/resources/client-stories/shoe-carnival";

const index$69 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$69,
   file: $$file$69,
   url: $$url$69
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$68 = createAstro();
const $$Index$68 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$68, $$props, $$slots);
  Astro2.self = $$Index$68;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/td-ameritrade/index.astro");

const $$file$68 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/td-ameritrade/index.astro";
const $$url$68 = "/resources/client-stories/td-ameritrade";

const index$68 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$68,
   file: $$file$68,
   url: $$url$68
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$67 = createAstro();
const $$Index$67 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$67, $$props, $$slots);
  Astro2.self = $$Index$67;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/cebu-pacific/index.astro");

const $$file$67 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/cebu-pacific/index.astro";
const $$url$67 = "/resources/client-stories/cebu-pacific";

const index$67 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$67,
   file: $$file$67,
   url: $$url$67
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$66 = createAstro();
const $$Index$66 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$66, $$props, $$slots);
  Astro2.self = $$Index$66;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/future-today/index.astro");

const $$file$66 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/future-today/index.astro";
const $$url$66 = "/resources/client-stories/future-today";

const index$66 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$66,
   file: $$file$66,
   url: $$url$66
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$65 = createAstro();
const $$Index$65 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$65, $$props, $$slots);
  Astro2.self = $$Index$65;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/network-next/index.astro");

const $$file$65 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/network-next/index.astro";
const $$url$65 = "/resources/client-stories/network-next";

const index$65 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$65,
   file: $$file$65,
   url: $$url$65
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$64 = createAstro();
const $$Index$64 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$64, $$props, $$slots);
  Astro2.self = $$Index$64;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/annie-selke/index.astro");

const $$file$64 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/annie-selke/index.astro";
const $$url$64 = "/resources/client-stories/annie-selke";

const index$64 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$64,
   file: $$file$64,
   url: $$url$64
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$63 = createAstro();
const $$Index$63 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$63, $$props, $$slots);
  Astro2.self = $$Index$63;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/coachguitar/index.astro");

const $$file$63 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/coachguitar/index.astro";
const $$url$63 = "/resources/client-stories/coachguitar";

const index$63 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$63,
   file: $$file$63,
   url: $$url$63
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$62 = createAstro();
const $$Index$62 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$62, $$props, $$slots);
  Astro2.self = $$Index$62;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/dailymotion/index.astro");

const $$file$62 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/dailymotion/index.astro";
const $$url$62 = "/resources/client-stories/dailymotion";

const index$62 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$62,
   file: $$file$62,
   url: $$url$62
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$61 = createAstro();
const $$Index$61 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$61, $$props, $$slots);
  Astro2.self = $$Index$61;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/e-w-scripps/index.astro");

const $$file$61 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/e-w-scripps/index.astro";
const $$url$61 = "/resources/client-stories/e-w-scripps";

const index$61 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$61,
   file: $$file$61,
   url: $$url$61
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$60 = createAstro();
const $$Index$60 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$60, $$props, $$slots);
  Astro2.self = $$Index$60;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/epic-sports/index.astro");

const $$file$60 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/epic-sports/index.astro";
const $$url$60 = "/resources/client-stories/epic-sports";

const index$60 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$60,
   file: $$file$60,
   url: $$url$60
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5$ = createAstro();
const $$Index$5$ = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5$, $$props, $$slots);
  Astro2.self = $$Index$5$;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/solvay-bank/index.astro");

const $$file$5$ = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/solvay-bank/index.astro";
const $$url$5$ = "/resources/client-stories/solvay-bank";

const index$5$ = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5$,
   file: $$file$5$,
   url: $$url$5$
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5_ = createAstro();
const $$Index$5_ = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5_, $$props, $$slots);
  Astro2.self = $$Index$5_;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/artstation/index.astro");

const $$file$5_ = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/artstation/index.astro";
const $$url$5_ = "/resources/client-stories/artstation";

const index$5_ = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5_,
   file: $$file$5_,
   url: $$url$5_
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5Z = createAstro();
const $$Index$5Z = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5Z, $$props, $$slots);
  Astro2.self = $$Index$5Z;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/cliplister/index.astro");

const $$file$5Z = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/cliplister/index.astro";
const $$url$5Z = "/resources/client-stories/cliplister";

const index$5Z = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5Z,
   file: $$file$5Z,
   url: $$url$5Z
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5Y = createAstro();
const $$Index$5Y = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5Y, $$props, $$slots);
  Astro2.self = $$Index$5Y;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/ntt-docomo/index.astro");

const $$file$5Y = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/ntt-docomo/index.astro";
const $$url$5Y = "/resources/client-stories/ntt-docomo";

const index$5Y = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5Y,
   file: $$file$5Y,
   url: $$url$5Y
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5X = createAstro();
const $$Index$5X = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5X, $$props, $$slots);
  Astro2.self = $$Index$5X;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/rakuten-tv/index.astro");

const $$file$5X = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/rakuten-tv/index.astro";
const $$url$5X = "/resources/client-stories/rakuten-tv";

const index$5X = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5X,
   file: $$file$5X,
   url: $$url$5X
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5W = createAstro();
const $$Index$5W = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5W, $$props, $$slots);
  Astro2.self = $$Index$5W;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/yare-media/index.astro");

const $$file$5W = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/yare-media/index.astro";
const $$url$5W = "/resources/client-stories/yare-media";

const index$5W = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5W,
   file: $$file$5W,
   url: $$url$5W
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5V = createAstro();
const $$Index$5V = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5V, $$props, $$slots);
  Astro2.self = $$Index$5V;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/primerica/index.astro");

const $$file$5V = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/primerica/index.astro";
const $$url$5V = "/resources/client-stories/primerica";

const index$5V = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5V,
   file: $$file$5V,
   url: $$url$5V
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5U = createAstro();
const $$Index$5U = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5U, $$props, $$slots);
  Astro2.self = $$Index$5U;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/rightmove/index.astro");

const $$file$5U = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/rightmove/index.astro";
const $$url$5U = "/resources/client-stories/rightmove";

const index$5U = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5U,
   file: $$file$5U,
   url: $$url$5U
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5T = createAstro();
const $$Index$5T = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5T, $$props, $$slots);
  Astro2.self = $$Index$5T;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/betradar/index.astro");

const $$file$5T = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/betradar/index.astro";
const $$url$5T = "/resources/client-stories/betradar";

const index$5T = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5T,
   file: $$file$5T,
   url: $$url$5T
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5S = createAstro();
const $$Index$5S = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5S, $$props, $$slots);
  Astro2.self = $$Index$5S;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/cinedigm/index.astro");

const $$file$5S = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/cinedigm/index.astro";
const $$url$5S = "/resources/client-stories/cinedigm";

const index$5S = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5S,
   file: $$file$5S,
   url: $$url$5S
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5R = createAstro();
const $$Index$5R = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5R, $$props, $$slots);
  Astro2.self = $$Index$5R;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/faurecia/index.astro");

const $$file$5R = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/faurecia/index.astro";
const $$url$5R = "/resources/client-stories/faurecia";

const index$5R = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5R,
   file: $$file$5R,
   url: $$url$5R
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5Q = createAstro();
const $$Index$5Q = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5Q, $$props, $$slots);
  Astro2.self = $$Index$5Q;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/symantec/index.astro");

const $$file$5Q = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/symantec/index.astro";
const $$url$5Q = "/resources/client-stories/symantec";

const index$5Q = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5Q,
   file: $$file$5Q,
   url: $$url$5Q
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5P = createAstro();
const $$Index$5P = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5P, $$props, $$slots);
  Astro2.self = $$Index$5P;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/tripwire/index.astro");

const $$file$5P = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/tripwire/index.astro";
const $$url$5P = "/resources/client-stories/tripwire";

const index$5P = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5P,
   file: $$file$5P,
   url: $$url$5P
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5O = createAstro();
const $$Index$5O = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5O, $$props, $$slots);
  Astro2.self = $$Index$5O;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/anyclip/index.astro");

const $$file$5O = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/anyclip/index.astro";
const $$url$5O = "/resources/client-stories/anyclip";

const index$5O = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5O,
   file: $$file$5O,
   url: $$url$5O
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5N = createAstro();
const $$Index$5N = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5N, $$props, $$slots);
  Astro2.self = $$Index$5N;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/appnext/index.astro");

const $$file$5N = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/appnext/index.astro";
const $$url$5N = "/resources/client-stories/appnext";

const index$5N = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5N,
   file: $$file$5N,
   url: $$url$5N
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5M = createAstro();
const $$Index$5M = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5M, $$props, $$slots);
  Astro2.self = $$Index$5M;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/asustek/index.astro");

const $$file$5M = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/asustek/index.astro";
const $$url$5M = "/resources/client-stories/asustek";

const index$5M = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5M,
   file: $$file$5M,
   url: $$url$5M
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5L = createAstro();
const $$Index$5L = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5L, $$props, $$slots);
  Astro2.self = $$Index$5L;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/betclic/index.astro");

const $$file$5L = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/betclic/index.astro";
const $$url$5L = "/resources/client-stories/betclic";

const index$5L = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5L,
   file: $$file$5L,
   url: $$url$5L
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5K = createAstro();
const $$Index$5K = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5K, $$props, $$slots);
  Astro2.self = $$Index$5K;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/iracing/index.astro");

const $$file$5K = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/iracing/index.astro";
const $$url$5K = "/resources/client-stories/iracing";

const index$5K = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5K,
   file: $$file$5K,
   url: $$url$5K
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5J = createAstro();
const $$Index$5J = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5J, $$props, $$slots);
  Astro2.self = $$Index$5J;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/napster/index.astro");

const $$file$5J = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/napster/index.astro";
const $$url$5J = "/resources/client-stories/napster";

const index$5J = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5J,
   file: $$file$5J,
   url: $$url$5J
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5I = createAstro();
const $$Index$5I = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5I, $$props, $$slots);
  Astro2.self = $$Index$5I;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/plarium/index.astro");

const $$file$5I = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/plarium/index.astro";
const $$url$5I = "/resources/client-stories/plarium";

const index$5I = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5I,
   file: $$file$5I,
   url: $$url$5I
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5H = createAstro();
const $$Index$5H = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5H, $$props, $$slots);
  Astro2.self = $$Index$5H;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/plus500/index.astro");

const $$file$5H = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/plus500/index.astro";
const $$url$5H = "/resources/client-stories/plus500";

const index$5H = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5H,
   file: $$file$5H,
   url: $$url$5H
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5G = createAstro();
const $$Index$5G = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5G, $$props, $$slots);
  Astro2.self = $$Index$5G;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/simultv/index.astro");

const $$file$5G = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/simultv/index.astro";
const $$url$5G = "/resources/client-stories/simultv";

const index$5G = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5G,
   file: $$file$5G,
   url: $$url$5G
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5F = createAstro();
const $$Index$5F = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5F, $$props, $$slots);
  Astro2.self = $$Index$5F;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/turnkey/index.astro");

const $$file$5F = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/turnkey/index.astro";
const $$url$5F = "/resources/client-stories/turnkey";

const index$5F = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5F,
   file: $$file$5F,
   url: $$url$5F
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5E = createAstro();
const $$Index$5E = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5E, $$props, $$slots);
  Astro2.self = $$Index$5E;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/verizon/index.astro");

const $$file$5E = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/verizon/index.astro";
const $$url$5E = "/resources/client-stories/verizon";

const index$5E = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5E,
   file: $$file$5E,
   url: $$url$5E
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5D = createAstro();
const $$Index$5D = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5D, $$props, $$slots);
  Astro2.self = $$Index$5D;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/dimoba/index.astro");

const $$file$5D = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/dimoba/index.astro";
const $$url$5D = "/resources/client-stories/dimoba";

const index$5D = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5D,
   file: $$file$5D,
   url: $$url$5D
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5C = createAstro();
const $$Index$5C = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5C, $$props, $$slots);
  Astro2.self = $$Index$5C;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/rumble/index.astro");

const $$file$5C = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/rumble/index.astro";
const $$url$5C = "/resources/client-stories/rumble";

const index$5C = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5C,
   file: $$file$5C,
   url: $$url$5C
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5B = createAstro();
const $$Index$5B = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5B, $$props, $$slots);
  Astro2.self = $$Index$5B;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/akira/index.astro");

const $$file$5B = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/akira/index.astro";
const $$url$5B = "/resources/client-stories/akira";

const index$5B = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5B,
   file: $$file$5B,
   url: $$url$5B
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5A = createAstro();
const $$Index$5A = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5A, $$props, $$slots);
  Astro2.self = $$Index$5A;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/byjus/index.astro");

const $$file$5A = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/byjus/index.astro";
const $$url$5A = "/resources/client-stories/byjus";

const index$5A = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5A,
   file: $$file$5A,
   url: $$url$5A
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5z = createAstro();
const $$Index$5z = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5z, $$props, $$slots);
  Astro2.self = $$Index$5z;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/byutv/index.astro");

const $$file$5z = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/byutv/index.astro";
const $$url$5z = "/resources/client-stories/byutv";

const index$5z = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5z,
   file: $$file$5z,
   url: $$url$5z
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5y = createAstro();
const $$Index$5y = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5y, $$props, $$slots);
  Astro2.self = $$Index$5y;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/chili/index.astro");

const $$file$5y = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/chili/index.astro";
const $$url$5y = "/resources/client-stories/chili";

const index$5y = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5y,
   file: $$file$5y,
   url: $$url$5y
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5x = createAstro();
const $$Index$5x = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5x, $$props, $$slots);
  Astro2.self = $$Index$5x;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/newsy/index.astro");

const $$file$5x = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/newsy/index.astro";
const $$url$5x = "/resources/client-stories/newsy";

const index$5x = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5x,
   file: $$file$5x,
   url: $$url$5x
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5w = createAstro();
const $$Index$5w = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5w, $$props, $$slots);
  Astro2.self = $$Index$5w;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/toppr/index.astro");

const $$file$5w = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/toppr/index.astro";
const $$url$5w = "/resources/client-stories/toppr";

const index$5w = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5w,
   file: $$file$5w,
   url: $$url$5w
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5v = createAstro();
const $$Index$5v = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5v, $$props, $$slots);
  Astro2.self = $$Index$5v;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/yahoo/index.astro");

const $$file$5v = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/yahoo/index.astro";
const $$url$5v = "/resources/client-stories/yahoo";

const index$5v = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5v,
   file: $$file$5v,
   url: $$url$5v
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5u = createAstro();
const $$Index$5u = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5u, $$props, $$slots);
  Astro2.self = $$Index$5u;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/zuken/index.astro");

const $$file$5u = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/zuken/index.astro";
const $$url$5u = "/resources/client-stories/zuken";

const index$5u = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5u,
   file: $$file$5u,
   url: $$url$5u
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5t = createAstro();
const $$Index$5t = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5t, $$props, $$slots);
  Astro2.self = $$Index$5t;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/cern/index.astro");

const $$file$5t = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/cern/index.astro";
const $$url$5t = "/resources/client-stories/cern";

const index$5t = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5t,
   file: $$file$5t,
   url: $$url$5t
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5s = createAstro();
const $$Index$5s = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5s, $$props, $$slots);
  Astro2.self = $$Index$5s;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/viki/index.astro");

const $$file$5s = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/viki/index.astro";
const $$url$5s = "/resources/client-stories/viki";

const index$5s = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5s,
   file: $$file$5s,
   url: $$url$5s
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5r = createAstro();
const $$Index$5r = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5r, $$props, $$slots);
  Astro2.self = $$Index$5r;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/bse/index.astro");

const $$file$5r = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/bse/index.astro";
const $$url$5r = "/resources/client-stories/bse";

const index$5r = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5r,
   file: $$file$5r,
   url: $$url$5r
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5q = createAstro();
const $$Index$5q = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5q, $$props, $$slots);
  Astro2.self = $$Index$5q;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/chl/index.astro");

const $$file$5q = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/chl/index.astro";
const $$url$5q = "/resources/client-stories/chl";

const index$5q = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5q,
   file: $$file$5q,
   url: $$url$5q
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5p = createAstro();
const $$Index$5p = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5p, $$props, $$slots);
  Astro2.self = $$Index$5p;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/tfo/index.astro");

const $$file$5p = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/tfo/index.astro";
const $$url$5p = "/resources/client-stories/tfo";

const index$5p = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5p,
   file: $$file$5p,
   url: $$url$5p
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5o = createAstro();
const $$Index$5o = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5o, $$props, $$slots);
  Astro2.self = $$Index$5o;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/tv5/index.astro");

const $$file$5o = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/client-stories/tv5/index.astro";
const $$url$5o = "/resources/client-stories/tv5";

const index$5o = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5o,
   file: $$file$5o,
   url: $$url$5o
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5n = createAstro();
const $$Index$5n = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5n, $$props, $$slots);
  Astro2.self = $$Index$5n;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/white-paper/5-things-multi-cdn-strategy/index.astro");

const $$file$5n = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/white-paper/5-things-multi-cdn-strategy/index.astro";
const $$url$5n = "/resources/white-paper/5-things-multi-cdn-strategy";

const index$5n = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5n,
   file: $$file$5n,
   url: $$url$5n
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5m = createAstro();
const $$Index$5m = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5m, $$props, $$slots);
  Astro2.self = $$Index$5m;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/white-paper/speed-scale-security/index.astro");

const $$file$5m = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/white-paper/speed-scale-security/index.astro";
const $$url$5m = "/resources/white-paper/speed-scale-security";

const index$5m = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5m,
   file: $$file$5m,
   url: $$url$5m
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5l = createAstro();
const $$Index$5l = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5l, $$props, $$slots);
  Astro2.self = $$Index$5l;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/index.astro");

const $$file$5l = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/index.astro";
const $$url$5l = "/resources/tutorials";

const index$5l = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5l,
   file: $$file$5l,
   url: $$url$5l
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5k = createAstro();
const $$Index$5k = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5k, $$props, $$slots);
  Astro2.self = $$Index$5k;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/applications-part-five-team-velocity-demo/index.astro");

const $$file$5k = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/applications-part-five-team-velocity-demo/index.astro";
const $$url$5k = "/resources/tutorials/applications-part-five-team-velocity-demo";

const index$5k = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5k,
   file: $$file$5k,
   url: $$url$5k
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5j = createAstro();
const $$Index$5j = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5j, $$props, $$slots);
  Astro2.self = $$Index$5j;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/applications-part-four-performance-demo/index.astro");

const $$file$5j = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/applications-part-four-performance-demo/index.astro";
const $$url$5j = "/resources/tutorials/applications-part-four-performance-demo";

const index$5j = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5j,
   file: $$file$5j,
   url: $$url$5j
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5i = createAstro();
const $$Index$5i = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5i, $$props, $$slots);
  Astro2.self = $$Index$5i;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/how-to-deploy-angular-ssr-apps-to-edgio/index.astro");

const $$file$5i = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/how-to-deploy-angular-ssr-apps-to-edgio/index.astro";
const $$url$5i = "/resources/tutorials/how-to-deploy-angular-ssr-apps-to-edgio";

const index$5i = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5i,
   file: $$file$5i,
   url: $$url$5i
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5h = createAstro();
const $$Index$5h = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5h, $$props, $$slots);
  Astro2.self = $$Index$5h;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/applications-part-three-security-demo/index.astro");

const $$file$5h = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/applications-part-three-security-demo/index.astro";
const $$url$5h = "/resources/tutorials/applications-part-three-security-demo";

const index$5h = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5h,
   file: $$file$5h,
   url: $$url$5h
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5g = createAstro();
const $$Index$5g = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5g, $$props, $$slots);
  Astro2.self = $$Index$5g;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/how-to-deploy-express-apps-with-edgio/index.astro");

const $$file$5g = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/how-to-deploy-express-apps-with-edgio/index.astro";
const $$url$5g = "/resources/tutorials/how-to-deploy-express-apps-with-edgio";

const index$5g = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5g,
   file: $$file$5g,
   url: $$url$5g
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5f = createAstro();
const $$Index$5f = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5f, $$props, $$slots);
  Astro2.self = $$Index$5f;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/how-to-deploy-nextjs-apps-with-edgio/index.astro");

const $$file$5f = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/how-to-deploy-nextjs-apps-with-edgio/index.astro";
const $$url$5f = "/resources/tutorials/how-to-deploy-nextjs-apps-with-edgio";

const index$5f = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5f,
   file: $$file$5f,
   url: $$url$5f
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5e = createAstro();
const $$Index$5e = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5e, $$props, $$slots);
  Astro2.self = $$Index$5e;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/applications-part-one-billions-lost/index.astro");

const $$file$5e = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/applications-part-one-billions-lost/index.astro";
const $$url$5e = "/resources/tutorials/applications-part-one-billions-lost";

const index$5e = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5e,
   file: $$file$5e,
   url: $$url$5e
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5d = createAstro();
const $$Index$5d = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5d, $$props, $$slots);
  Astro2.self = $$Index$5d;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/how-to-deploy-nitro-apps-to-edgio/index.astro");

const $$file$5d = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/how-to-deploy-nitro-apps-to-edgio/index.astro";
const $$url$5d = "/resources/tutorials/how-to-deploy-nitro-apps-to-edgio";

const index$5d = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5d,
   file: $$file$5d,
   url: $$url$5d
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5c = createAstro();
const $$Index$5c = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5c, $$props, $$slots);
  Astro2.self = $$Index$5c;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/how-to-deploy-nuxt3-apps-to-edgio/index.astro");

const $$file$5c = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/how-to-deploy-nuxt3-apps-to-edgio/index.astro";
const $$url$5c = "/resources/tutorials/how-to-deploy-nuxt3-apps-to-edgio";

const index$5c = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5c,
   file: $$file$5c,
   url: $$url$5c
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5b = createAstro();
const $$Index$5b = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5b, $$props, $$slots);
  Astro2.self = $$Index$5b;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/how-to-deploy-hexo-apps-to-edgio/index.astro");

const $$file$5b = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/how-to-deploy-hexo-apps-to-edgio/index.astro";
const $$url$5b = "/resources/tutorials/how-to-deploy-hexo-apps-to-edgio";

const index$5b = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5b,
   file: $$file$5b,
   url: $$url$5b
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5a = createAstro();
const $$Index$5a = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5a, $$props, $$slots);
  Astro2.self = $$Index$5a;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/how-to-deploy-cra-apps-to-edgio/index.astro");

const $$file$5a = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/how-to-deploy-cra-apps-to-edgio/index.astro";
const $$url$5a = "/resources/tutorials/how-to-deploy-cra-apps-to-edgio";

const index$5a = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5a,
   file: $$file$5a,
   url: $$url$5a
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$59 = createAstro();
const $$Index$59 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$59, $$props, $$slots);
  Astro2.self = $$Index$59;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/applications-part-six-summary/index.astro");

const $$file$59 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/applications-part-six-summary/index.astro";
const $$url$59 = "/resources/tutorials/applications-part-six-summary";

const index$59 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$59,
   file: $$file$59,
   url: $$url$59
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$58 = createAstro();
const $$Index$58 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$58, $$props, $$slots);
  Astro2.self = $$Index$58;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/applications-part-two-product/index.astro");

const $$file$58 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/applications-part-two-product/index.astro";
const $$url$58 = "/resources/tutorials/applications-part-two-product";

const index$58 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$58,
   file: $$file$58,
   url: $$url$58
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$57 = createAstro();
const $$Index$57 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$57, $$props, $$slots);
  Astro2.self = $$Index$57;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/getting-started-on-edgio/index.astro");

const $$file$57 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/getting-started-on-edgio/index.astro";
const $$url$57 = "/resources/tutorials/getting-started-on-edgio";

const index$57 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$57,
   file: $$file$57,
   url: $$url$57
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$56 = createAstro();
const $$Index$56 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$56, $$props, $$slots);
  Astro2.self = $$Index$56;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/edgio-v5-migration/index.astro");

const $$file$56 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/tutorials/edgio-v5-migration/index.astro";
const $$url$56 = "/resources/tutorials/edgio-v5-migration";

const index$56 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$56,
   file: $$file$56,
   url: $$url$56
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$55 = createAstro();
const $$Index$55 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$55, $$props, $$slots);
  Astro2.self = $$Index$55;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/ebook/playbook-for-delivering-high-profile-streaming-events/index.astro");

const $$file$55 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/ebook/playbook-for-delivering-high-profile-streaming-events/index.astro";
const $$url$55 = "/resources/ebook/playbook-for-delivering-high-profile-streaming-events";

const index$55 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$55,
   file: $$file$55,
   url: $$url$55
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$54 = createAstro();
const $$Index$54 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$54, $$props, $$slots);
  Astro2.self = $$Index$54;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/ebook/turning-on-a-dime/index.astro");

const $$file$54 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/ebook/turning-on-a-dime/index.astro";
const $$url$54 = "/resources/ebook/turning-on-a-dime";

const index$54 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$54,
   file: $$file$54,
   url: $$url$54
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$53 = createAstro();
const $$Index$53 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$53, $$props, $$slots);
  Astro2.self = $$Index$53;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/index.astro");

const $$file$53 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/index.astro";
const $$url$53 = "/resources/blog";

const index$53 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$53,
   file: $$file$53,
   url: $$url$53
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$52 = createAstro();
const $$Index$52 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$52, $$props, $$slots);
  Astro2.self = $$Index$52;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/reduce-bounce-rates-and-improve-site-experience-with-predictive-prefetching/index.astro");

const $$file$52 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/reduce-bounce-rates-and-improve-site-experience-with-predictive-prefetching/index.astro";
const $$url$52 = "/resources/blog/reduce-bounce-rates-and-improve-site-experience-with-predictive-prefetching";

const index$52 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$52,
   file: $$file$52,
   url: $$url$52
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$51 = createAstro();
const $$Index$51 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$51, $$props, $$slots);
  Astro2.self = $$Index$51;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/flexibility-and-playback-scale-is-essential-for-ultimate-stream-management/index.astro");

const $$file$51 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/flexibility-and-playback-scale-is-essential-for-ultimate-stream-management/index.astro";
const $$url$51 = "/resources/blog/flexibility-and-playback-scale-is-essential-for-ultimate-stream-management";

const index$51 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$51,
   file: $$file$51,
   url: $$url$51
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$50 = createAstro();
const $$Index$50 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$50, $$props, $$slots);
  Astro2.self = $$Index$50;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/google-data-reveals-2020-website-speed-benchmarks-for-ecommerce-websites/index.astro");

const $$file$50 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/google-data-reveals-2020-website-speed-benchmarks-for-ecommerce-websites/index.astro";
const $$url$50 = "/resources/blog/google-data-reveals-2020-website-speed-benchmarks-for-ecommerce-websites";

const index$50 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$50,
   file: $$file$50,
   url: $$url$50
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4$ = createAstro();
const $$Index$4$ = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4$, $$props, $$slots);
  Astro2.self = $$Index$4$;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/network-performance-and-optimizations-are-essential-for-viewership-scale/index.astro");

const $$file$4$ = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/network-performance-and-optimizations-are-essential-for-viewership-scale/index.astro";
const $$url$4$ = "/resources/blog/network-performance-and-optimizations-are-essential-for-viewership-scale";

const index$4$ = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4$,
   file: $$file$4$,
   url: $$url$4$
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4_ = createAstro();
const $$Index$4_ = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4_, $$props, $$slots);
  Astro2.self = $$Index$4_;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/debunking-conventional-wisdom-increased-security-improves-performance/index.astro");

const $$file$4_ = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/debunking-conventional-wisdom-increased-security-improves-performance/index.astro";
const $$url$4_ = "/resources/blog/debunking-conventional-wisdom-increased-security-improves-performance";

const index$4_ = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4_,
   file: $$file$4_,
   url: $$url$4_
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4Z = createAstro();
const $$Index$4Z = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4Z, $$props, $$slots);
  Astro2.self = $$Index$4Z;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/four-reasons-to-outsource-large-it-projects-during-economic-headwinds/index.astro");

const $$file$4Z = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/four-reasons-to-outsource-large-it-projects-during-economic-headwinds/index.astro";
const $$url$4Z = "/resources/blog/four-reasons-to-outsource-large-it-projects-during-economic-headwinds";

const index$4Z = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4Z,
   file: $$file$4Z,
   url: $$url$4Z
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4Y = createAstro();
const $$Index$4Y = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4Y, $$props, $$slots);
  Astro2.self = $$Index$4Y;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/maximizing-cdn-performance-for-your-application-with-edgejs-by-edgio/index.astro");

const $$file$4Y = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/maximizing-cdn-performance-for-your-application-with-edgejs-by-edgio/index.astro";
const $$url$4Y = "/resources/blog/maximizing-cdn-performance-for-your-application-with-edgejs-by-edgio";

const index$4Y = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4Y,
   file: $$file$4Y,
   url: $$url$4Y
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4X = createAstro();
const $$Index$4X = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4X, $$props, $$slots);
  Astro2.self = $$Index$4X;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/cloudflare-vs-cloudfront-vs-layer0-a-comparison-of-the-leading-cdns/index.astro");

const $$file$4X = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/cloudflare-vs-cloudfront-vs-layer0-a-comparison-of-the-leading-cdns/index.astro";
const $$url$4X = "/resources/blog/cloudflare-vs-cloudfront-vs-layer0-a-comparison-of-the-leading-cdns";

const index$4X = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4X,
   file: $$file$4X,
   url: $$url$4X
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4W = createAstro();
const $$Index$4W = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4W, $$props, $$slots);
  Astro2.self = $$Index$4W;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edge-insights-enables-real-time-security-and-performance-visbility/index.astro");

const $$file$4W = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edge-insights-enables-real-time-security-and-performance-visbility/index.astro";
const $$url$4W = "/resources/blog/edge-insights-enables-real-time-security-and-performance-visbility";

const index$4W = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4W,
   file: $$file$4W,
   url: $$url$4W
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4V = createAstro();
const $$Index$4V = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4V, $$props, $$slots);
  Astro2.self = $$Index$4V;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/reduce-vulnerabilities-and-defend-your-brand-against-ddos-attacks/index.astro");

const $$file$4V = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/reduce-vulnerabilities-and-defend-your-brand-against-ddos-attacks/index.astro";
const $$url$4V = "/resources/blog/reduce-vulnerabilities-and-defend-your-brand-against-ddos-attacks";

const index$4V = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4V,
   file: $$file$4V,
   url: $$url$4V
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4U = createAstro();
const $$Index$4U = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4U, $$props, $$slots);
  Astro2.self = $$Index$4U;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/four-key-things-you-need-to-know-about-implementing-a-multi-CDN/index.astro");

const $$file$4U = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/four-key-things-you-need-to-know-about-implementing-a-multi-CDN/index.astro";
const $$url$4U = "/resources/blog/four-key-things-you-need-to-know-about-implementing-a-multi-CDN";

const index$4U = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4U,
   file: $$file$4U,
   url: $$url$4U
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4T = createAstro();
const $$Index$4T = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4T, $$props, $$slots);
  Astro2.self = $$Index$4T;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/introducing-serverless-deep-request-inspection-for-the-jamstack/index.astro");

const $$file$4T = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/introducing-serverless-deep-request-inspection-for-the-jamstack/index.astro";
const $$url$4T = "/resources/blog/introducing-serverless-deep-request-inspection-for-the-jamstack";

const index$4T = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4T,
   file: $$file$4T,
   url: $$url$4T
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4S = createAstro();
const $$Index$4S = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4S, $$props, $$slots);
  Astro2.self = $$Index$4S;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/improve-vod-playback-performance-with-virtual-linear-playlists/index.astro");

const $$file$4S = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/improve-vod-playback-performance-with-virtual-linear-playlists/index.astro";
const $$url$4S = "/resources/blog/improve-vod-playback-performance-with-virtual-linear-playlists";

const index$4S = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4S,
   file: $$file$4S,
   url: $$url$4S
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4R = createAstro();
const $$Index$4R = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4R, $$props, $$slots);
  Astro2.self = $$Index$4R;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/lighthouse-6-0-score-audit-a-checklist-to-prepare-your-website/index.astro");

const $$file$4R = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/lighthouse-6-0-score-audit-a-checklist-to-prepare-your-website/index.astro";
const $$url$4R = "/resources/blog/lighthouse-6-0-score-audit-a-checklist-to-prepare-your-website";

const index$4R = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4R,
   file: $$file$4R,
   url: $$url$4R
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4Q = createAstro();
const $$Index$4Q = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4Q, $$props, $$slots);
  Astro2.self = $$Index$4Q;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/real-time-log-delivery-now-supports-both-cdn-and-security-logs/index.astro");

const $$file$4Q = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/real-time-log-delivery-now-supports-both-cdn-and-security-logs/index.astro";
const $$url$4Q = "/resources/blog/real-time-log-delivery-now-supports-both-cdn-and-security-logs";

const index$4Q = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4Q,
   file: $$file$4Q,
   url: $$url$4Q
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4P = createAstro();
const $$Index$4P = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4P, $$props, $$slots);
  Astro2.self = $$Index$4P;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/three-critical-web-app-vulnerabilities-you-should-address-now/index.astro");

const $$file$4P = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/three-critical-web-app-vulnerabilities-you-should-address-now/index.astro";
const $$url$4P = "/resources/blog/three-critical-web-app-vulnerabilities-you-should-address-now";

const index$4P = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4P,
   file: $$file$4P,
   url: $$url$4P
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4O = createAstro();
const $$Index$4O = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4O, $$props, $$slots);
  Astro2.self = $$Index$4O;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/building-your-own-web-application-platform-is-locking-you-in/index.astro");

const $$file$4O = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/building-your-own-web-application-platform-is-locking-you-in/index.astro";
const $$url$4O = "/resources/blog/building-your-own-web-application-platform-is-locking-you-in";

const index$4O = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4O,
   file: $$file$4O,
   url: $$url$4O
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4N = createAstro();
const $$Index$4N = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4N, $$props, $$slots);
  Astro2.self = $$Index$4N;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/improving-application-performance-with-faster-security-rules/index.astro");

const $$file$4N = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/improving-application-performance-with-faster-security-rules/index.astro";
const $$url$4N = "/resources/blog/improving-application-performance-with-faster-security-rules";

const index$4N = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4N,
   file: $$file$4N,
   url: $$url$4N
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4M = createAstro();
const $$Index$4M = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4M, $$props, $$slots);
  Astro2.self = $$Index$4M;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/100-leading-ecommerce-retailers-average-a-lower-performance/index.astro");

const $$file$4M = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/100-leading-ecommerce-retailers-average-a-lower-performance/index.astro";
const $$url$4M = "/resources/blog/100-leading-ecommerce-retailers-average-a-lower-performance";

const index$4M = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4M,
   file: $$file$4M,
   url: $$url$4M
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4L = createAstro();
const $$Index$4L = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4L, $$props, $$slots);
  Astro2.self = $$Index$4L;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/7-ways-edge-innovations-are-transforming-online-experiences/index.astro");

const $$file$4L = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/7-ways-edge-innovations-are-transforming-online-experiences/index.astro";
const $$url$4L = "/resources/blog/7-ways-edge-innovations-are-transforming-online-experiences";

const index$4L = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4L,
   file: $$file$4L,
   url: $$url$4L
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4K = createAstro();
const $$Index$4K = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4K, $$props, $$slots);
  Astro2.self = $$Index$4K;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/delivering-the-bloomreach-experience-at-the-speed-of-layer0/index.astro");

const $$file$4K = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/delivering-the-bloomreach-experience-at-the-speed-of-layer0/index.astro";
const $$url$4K = "/resources/blog/delivering-the-bloomreach-experience-at-the-speed-of-layer0";

const index$4K = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4K,
   file: $$file$4K,
   url: $$url$4K
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4J = createAstro();
const $$Index$4J = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4J, $$props, $$slots);
  Astro2.self = $$Index$4J;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-to-choose-the-right-hosting-provider-for-your-ecommerce/index.astro");

const $$file$4J = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-to-choose-the-right-hosting-provider-for-your-ecommerce/index.astro";
const $$url$4J = "/resources/blog/how-to-choose-the-right-hosting-provider-for-your-ecommerce";

const index$4J = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4J,
   file: $$file$4J,
   url: $$url$4J
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4I = createAstro();
const $$Index$4I = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4I, $$props, $$slots);
  Astro2.self = $$Index$4I;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-to-detect-and-mitigate-the-latest-cybersecurity-threats/index.astro");

const $$file$4I = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-to-detect-and-mitigate-the-latest-cybersecurity-threats/index.astro";
const $$url$4I = "/resources/blog/how-to-detect-and-mitigate-the-latest-cybersecurity-threats";

const index$4I = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4I,
   file: $$file$4I,
   url: $$url$4I
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4H = createAstro();
const $$Index$4H = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4H, $$props, $$slots);
  Astro2.self = $$Index$4H;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/layer0-joins-the-mach-alliance-to-help-advance-the-movement/index.astro");

const $$file$4H = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/layer0-joins-the-mach-alliance-to-help-advance-the-movement/index.astro";
const $$url$4H = "/resources/blog/layer0-joins-the-mach-alliance-to-help-advance-the-movement";

const index$4H = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4H,
   file: $$file$4H,
   url: $$url$4H
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4G = createAstro();
const $$Index$4G = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4G, $$props, $$slots);
  Astro2.self = $$Index$4G;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/limelight-announces-general-availability-of-graphql-caching/index.astro");

const $$file$4G = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/limelight-announces-general-availability-of-graphql-caching/index.astro";
const $$url$4G = "/resources/blog/limelight-announces-general-availability-of-graphql-caching";

const index$4G = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4G,
   file: $$file$4G,
   url: $$url$4G
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4F = createAstro();
const $$Index$4F = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4F, $$props, $$slots);
  Astro2.self = $$Index$4F;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/ott-providers-reveal-how-security-impacts-viewer-experience/index.astro");

const $$file$4F = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/ott-providers-reveal-how-security-impacts-viewer-experience/index.astro";
const $$url$4F = "/resources/blog/ott-providers-reveal-how-security-impacts-viewer-experience";

const index$4F = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4F,
   file: $$file$4F,
   url: $$url$4F
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4E = createAstro();
const $$Index$4E = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4E, $$props, $$slots);
  Astro2.self = $$Index$4E;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/87-percent-ecommerce-websites-fail-googles-core-web-vitals/index.astro");

const $$file$4E = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/87-percent-ecommerce-websites-fail-googles-core-web-vitals/index.astro";
const $$url$4E = "/resources/blog/87-percent-ecommerce-websites-fail-googles-core-web-vitals";

const index$4E = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4E,
   file: $$file$4E,
   url: $$url$4E
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4D = createAstro();
const $$Index$4D = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4D, $$props, $$slots);
  Astro2.self = $$Index$4D;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/beyond-optimization-the-secrets-behind-the-fastest-magento/index.astro");

const $$file$4D = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/beyond-optimization-the-secrets-behind-the-fastest-magento/index.astro";
const $$url$4D = "/resources/blog/beyond-optimization-the-secrets-behind-the-fastest-magento";

const index$4D = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4D,
   file: $$file$4D,
   url: $$url$4D
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4C = createAstro();
const $$Index$4C = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4C, $$props, $$slots);
  Astro2.self = $$Index$4C;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edgio-earns-cyber-defense-global-infosec-award-at-rsa-2023/index.astro");

const $$file$4C = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edgio-earns-cyber-defense-global-infosec-award-at-rsa-2023/index.astro";
const $$url$4C = "/resources/blog/edgio-earns-cyber-defense-global-infosec-award-at-rsa-2023";

const index$4C = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4C,
   file: $$file$4C,
   url: $$url$4C
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4B = createAstro();
const $$Index$4B = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4B, $$props, $$slots);
  Astro2.self = $$Index$4B;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-layer0-automatically-generates-amp-from-your-react-pwa/index.astro");

const $$file$4B = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-layer0-automatically-generates-amp-from-your-react-pwa/index.astro";
const $$url$4B = "/resources/blog/how-layer0-automatically-generates-amp-from-your-react-pwa";

const index$4B = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4B,
   file: $$file$4B,
   url: $$url$4B
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4A = createAstro();
const $$Index$4A = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4A, $$props, $$slots);
  Astro2.self = $$Index$4A;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/looking-for-better-website-performance-start-with-your-waf/index.astro");

const $$file$4A = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/looking-for-better-website-performance-start-with-your-waf/index.astro";
const $$url$4A = "/resources/blog/looking-for-better-website-performance-start-with-your-waf";

const index$4A = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4A,
   file: $$file$4A,
   url: $$url$4A
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4z = createAstro();
const $$Index$4z = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4z, $$props, $$slots);
  Astro2.self = $$Index$4z;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edtech-and-video-content-delivery-ends-need-for-snow-days/index.astro");

const $$file$4z = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edtech-and-video-content-delivery-ends-need-for-snow-days/index.astro";
const $$url$4z = "/resources/blog/edtech-and-video-content-delivery-ends-need-for-snow-days";

const index$4z = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4z,
   file: $$file$4z,
   url: $$url$4z
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4y = createAstro();
const $$Index$4y = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4y, $$props, $$slots);
  Astro2.self = $$Index$4y;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/limelight-reports-strong-financial-results-for-q3-of-2021/index.astro");

const $$file$4y = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/limelight-reports-strong-financial-results-for-q3-of-2021/index.astro";
const $$url$4y = "/resources/blog/limelight-reports-strong-financial-results-for-q3-of-2021";

const index$4y = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4y,
   file: $$file$4y,
   url: $$url$4y
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4x = createAstro();
const $$Index$4x = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4x, $$props, $$slots);
  Astro2.self = $$Index$4x;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/speed-sells-ecommerce-uses-amp-ssr-pwa-and-edge-computing/index.astro");

const $$file$4x = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/speed-sells-ecommerce-uses-amp-ssr-pwa-and-edge-computing/index.astro";
const $$url$4x = "/resources/blog/speed-sells-ecommerce-uses-amp-ssr-pwa-and-edge-computing";

const index$4x = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4x,
   file: $$file$4x,
   url: $$url$4x
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4w = createAstro();
const $$Index$4w = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4w, $$props, $$slots);
  Astro2.self = $$Index$4w;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/using-layer0s-edgejs-to-configure-custom-security-headers/index.astro");

const $$file$4w = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/using-layer0s-edgejs-to-configure-custom-security-headers/index.astro";
const $$url$4w = "/resources/blog/using-layer0s-edgejs-to-configure-custom-security-headers";

const index$4w = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4w,
   file: $$file$4w,
   url: $$url$4w
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4v = createAstro();
const $$Index$4v = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4v, $$props, $$slots);
  Astro2.self = $$Index$4v;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/which-modern-frontend-is-the-fastest-react-angular-vue-js/index.astro");

const $$file$4v = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/which-modern-frontend-is-the-fastest-react-angular-vue-js/index.astro";
const $$url$4v = "/resources/blog/which-modern-frontend-is-the-fastest-react-angular-vue-js";

const index$4v = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4v,
   file: $$file$4v,
   url: $$url$4v
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4u = createAstro();
const $$Index$4u = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4u, $$props, $$slots);
  Astro2.self = $$Index$4u;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/azure-cdn-from-verizon-delivers-virtual-ces-to-audiences/index.astro");

const $$file$4u = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/azure-cdn-from-verizon-delivers-virtual-ces-to-audiences/index.astro";
const $$url$4u = "/resources/blog/azure-cdn-from-verizon-delivers-virtual-ces-to-audiences";

const index$4u = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4u,
   file: $$file$4u,
   url: $$url$4u
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4t = createAstro();
const $$Index$4t = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4t, $$props, $$slots);
  Astro2.self = $$Index$4t;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/future-of-cybersecurity-edgio-observations-from-rsa-2023/index.astro");

const $$file$4t = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/future-of-cybersecurity-edgio-observations-from-rsa-2023/index.astro";
const $$url$4t = "/resources/blog/future-of-cybersecurity-edgio-observations-from-rsa-2023";

const index$4t = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4t,
   file: $$file$4t,
   url: $$url$4t
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4s = createAstro();
const $$Index$4s = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4s, $$props, $$slots);
  Astro2.self = $$Index$4s;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/limelight-adds-live-push-ingest-for-live-stream-delivery/index.astro");

const $$file$4s = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/limelight-adds-live-push-ingest-for-live-stream-delivery/index.astro";
const $$url$4s = "/resources/blog/limelight-adds-live-push-ingest-for-live-stream-delivery";

const index$4s = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4s,
   file: $$file$4s,
   url: $$url$4s
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4r = createAstro();
const $$Index$4r = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4r, $$props, $$slots);
  Astro2.self = $$Index$4r;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/optimizing-react-angular-or-vue-single-page-applications/index.astro");

const $$file$4r = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/optimizing-react-angular-or-vue-single-page-applications/index.astro";
const $$url$4r = "/resources/blog/optimizing-react-angular-or-vue-single-page-applications";

const index$4r = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4r,
   file: $$file$4r,
   url: $$url$4r
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4q = createAstro();
const $$Index$4q = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4q, $$props, $$slots);
  Astro2.self = $$Index$4q;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/rules-engine-rules-to-live-by-for-better-cdn-performance/index.astro");

const $$file$4q = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/rules-engine-rules-to-live-by-for-better-cdn-performance/index.astro";
const $$url$4q = "/resources/blog/rules-engine-rules-to-live-by-for-better-cdn-performance";

const index$4q = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4q,
   file: $$file$4q,
   url: $$url$4q
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4p = createAstro();
const $$Index$4p = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4p, $$props, $$slots);
  Astro2.self = $$Index$4p;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/cdn-edge-brings-compute-closer-to-where-its-needed-most/index.astro");

const $$file$4p = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/cdn-edge-brings-compute-closer-to-where-its-needed-most/index.astro";
const $$url$4p = "/resources/blog/cdn-edge-brings-compute-closer-to-where-its-needed-most";

const index$4p = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4p,
   file: $$file$4p,
   url: $$url$4p
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4o = createAstro();
const $$Index$4o = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4o, $$props, $$slots);
  Astro2.self = $$Index$4o;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edgio-reports-second-quarter-financial-results-for-2022/index.astro");

const $$file$4o = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edgio-reports-second-quarter-financial-results-for-2022/index.astro";
const $$url$4o = "/resources/blog/edgio-reports-second-quarter-financial-results-for-2022";

const index$4o = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4o,
   file: $$file$4o,
   url: $$url$4o
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4n = createAstro();
const $$Index$4n = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4n, $$props, $$slots);
  Astro2.self = $$Index$4n;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-frontend-developers-can-fix-cumulative-layout-shift/index.astro");

const $$file$4n = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-frontend-developers-can-fix-cumulative-layout-shift/index.astro";
const $$url$4n = "/resources/blog/how-frontend-developers-can-fix-cumulative-layout-shift";

const index$4n = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4n,
   file: $$file$4n,
   url: $$url$4n
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4m = createAstro();
const $$Index$4m = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4m, $$props, $$slots);
  Astro2.self = $$Index$4m;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-to-evaluate-it-investments-during-economic-downturn/index.astro");

const $$file$4m = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-to-evaluate-it-investments-during-economic-downturn/index.astro";
const $$url$4m = "/resources/blog/how-to-evaluate-it-investments-during-economic-downturn";

const index$4m = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4m,
   file: $$file$4m,
   url: $$url$4m
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4l = createAstro();
const $$Index$4l = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4l, $$props, $$slots);
  Astro2.self = $$Index$4l;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/improve-web-app-performance-and-security-for-higher-roi/index.astro");

const $$file$4l = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/improve-web-app-performance-and-security-for-higher-roi/index.astro";
const $$url$4l = "/resources/blog/improve-web-app-performance-and-security-for-higher-roi";

const index$4l = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4l,
   file: $$file$4l,
   url: $$url$4l
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4k = createAstro();
const $$Index$4k = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4k, $$props, $$slots);
  Astro2.self = $$Index$4k;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/limelight-launches-new-solution-in-web-cdn-and-security/index.astro");

const $$file$4k = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/limelight-launches-new-solution-in-web-cdn-and-security/index.astro";
const $$url$4k = "/resources/blog/limelight-launches-new-solution-in-web-cdn-and-security";

const index$4k = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4k,
   file: $$file$4k,
   url: $$url$4k
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4j = createAstro();
const $$Index$4j = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4j, $$props, $$slots);
  Astro2.self = $$Index$4j;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/limelight-networks-reports-record-first-quarter-results/index.astro");

const $$file$4j = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/limelight-networks-reports-record-first-quarter-results/index.astro";
const $$url$4j = "/resources/blog/limelight-networks-reports-record-first-quarter-results";

const index$4j = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4j,
   file: $$file$4j,
   url: $$url$4j
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4i = createAstro();
const $$Index$4i = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4i, $$props, $$slots);
  Astro2.self = $$Index$4i;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/manage-your-edgio-platform-configuration-with-terraform/index.astro");

const $$file$4i = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/manage-your-edgio-platform-configuration-with-terraform/index.astro";
const $$url$4i = "/resources/blog/manage-your-edgio-platform-configuration-with-terraform";

const index$4i = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4i,
   file: $$file$4i,
   url: $$url$4i
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4h = createAstro();
const $$Index$4h = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4h, $$props, $$slots);
  Astro2.self = $$Index$4h;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/operational-scale-is-essential-for-successful-streaming/index.astro");

const $$file$4h = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/operational-scale-is-essential-for-successful-streaming/index.astro";
const $$url$4h = "/resources/blog/operational-scale-is-essential-for-successful-streaming";

const index$4h = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4h,
   file: $$file$4h,
   url: $$url$4h
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4g = createAstro();
const $$Index$4g = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4g, $$props, $$slots);
  Astro2.self = $$Index$4g;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/the-new-layer0-docs-is-the-beginning-of-a-new-beginning/index.astro");

const $$file$4g = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/the-new-layer0-docs-is-the-beginning-of-a-new-beginning/index.astro";
const $$url$4g = "/resources/blog/the-new-layer0-docs-is-the-beginning-of-a-new-beginning";

const index$4g = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4g,
   file: $$file$4g,
   url: $$url$4g
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4f = createAstro();
const $$Index$4f = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4f, $$props, $$slots);
  Astro2.self = $$Index$4f;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/top-progressive-web-app-examples-tinder-twitter-walmart/index.astro");

const $$file$4f = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/top-progressive-web-app-examples-tinder-twitter-walmart/index.astro";
const $$url$4f = "/resources/blog/top-progressive-web-app-examples-tinder-twitter-walmart";

const index$4f = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4f,
   file: $$file$4f,
   url: $$url$4f
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4e = createAstro();
const $$Index$4e = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4e, $$props, $$slots);
  Astro2.self = $$Index$4e;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/accelerating-momentum-a-progress-update-and-look-ahead/index.astro");

const $$file$4e = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/accelerating-momentum-a-progress-update-and-look-ahead/index.astro";
const $$url$4e = "/resources/blog/accelerating-momentum-a-progress-update-and-look-ahead";

const index$4e = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4e,
   file: $$file$4e,
   url: $$url$4e
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4d = createAstro();
const $$Index$4d = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4d, $$props, $$slots);
  Astro2.self = $$Index$4d;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/cache-graphql-apis-at-the-edge-with-serverless-hosting/index.astro");

const $$file$4d = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/cache-graphql-apis-at-the-edge-with-serverless-hosting/index.astro";
const $$url$4d = "/resources/blog/cache-graphql-apis-at-the-edge-with-serverless-hosting";

const index$4d = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4d,
   file: $$file$4d,
   url: $$url$4d
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4c = createAstro();
const $$Index$4c = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4c, $$props, $$slots);
  Astro2.self = $$Index$4c;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/cross-site-scripting-attacks-trends-and-best-practices/index.astro");

const $$file$4c = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/cross-site-scripting-attacks-trends-and-best-practices/index.astro";
const $$url$4c = "/resources/blog/cross-site-scripting-attacks-trends-and-best-practices";

const index$4c = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4c,
   file: $$file$4c,
   url: $$url$4c
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4b = createAstro();
const $$Index$4b = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4b, $$props, $$slots);
  Astro2.self = $$Index$4b;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-to-optimize-core-web-vitals-for-ecommerce-websites/index.astro");

const $$file$4b = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-to-optimize-core-web-vitals-for-ecommerce-websites/index.astro";
const $$url$4b = "/resources/blog/how-to-optimize-core-web-vitals-for-ecommerce-websites";

const index$4b = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4b,
   file: $$file$4b,
   url: $$url$4b
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4a = createAstro();
const $$Index$4a = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4a, $$props, $$slots);
  Astro2.self = $$Index$4a;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/layer0-security-new-waf-and-bot-detection-capabilities/index.astro");

const $$file$4a = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/layer0-security-new-waf-and-bot-detection-capabilities/index.astro";
const $$url$4a = "/resources/blog/layer0-security-new-waf-and-bot-detection-capabilities";

const index$4a = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4a,
   file: $$file$4a,
   url: $$url$4a
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$49 = createAstro();
const $$Index$49 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$49, $$props, $$slots);
  Astro2.self = $$Index$49;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/three-ways-to-invest-in-it-during-an-economic-downturn/index.astro");

const $$file$49 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/three-ways-to-invest-in-it-during-an-economic-downturn/index.astro";
const $$url$49 = "/resources/blog/three-ways-to-invest-in-it-during-an-economic-downturn";

const index$49 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$49,
   file: $$file$49,
   url: $$url$49
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$48 = createAstro();
const $$Index$48 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$48, $$props, $$slots);
  Astro2.self = $$Index$48;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/azureplatform-improve-cdn-performance-and-lower-costs/index.astro");

const $$file$48 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/azureplatform-improve-cdn-performance-and-lower-costs/index.astro";
const $$url$48 = "/resources/blog/azureplatform-improve-cdn-performance-and-lower-costs";

const index$48 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$48,
   file: $$file$48,
   url: $$url$48
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$47 = createAstro();
const $$Index$47 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$47, $$props, $$slots);
  Astro2.self = $$Index$47;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-to-increase-viewer-engagement-with-virtual-linear/index.astro");

const $$file$47 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-to-increase-viewer-engagement-with-virtual-linear/index.astro";
const $$url$47 = "/resources/blog/how-to-increase-viewer-engagement-with-virtual-linear";

const index$47 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$47,
   file: $$file$47,
   url: $$url$47
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$46 = createAstro();
const $$Index$46 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$46, $$props, $$slots);
  Astro2.self = $$Index$46;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/limelight-grants-equity-awards-to-moov-corp-employees/index.astro");

const $$file$46 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/limelight-grants-equity-awards-to-moov-corp-employees/index.astro";
const $$url$46 = "/resources/blog/limelight-grants-equity-awards-to-moov-corp-employees";

const index$46 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$46,
   file: $$file$46,
   url: $$url$46
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$45 = createAstro();
const $$Index$45 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$45, $$props, $$slots);
  Astro2.self = $$Index$45;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/low-latency-live-streaming-is-key-to-capturing-online/index.astro");

const $$file$45 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/low-latency-live-streaming-is-key-to-capturing-online/index.astro";
const $$url$45 = "/resources/blog/low-latency-live-streaming-is-key-to-capturing-online";

const index$45 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$45,
   file: $$file$45,
   url: $$url$45
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$44 = createAstro();
const $$Index$44 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$44, $$props, $$slots);
  Astro2.self = $$Index$44;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/taking-media-to-the-edge-harnessing-the-power-of-edge/index.astro");

const $$file$44 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/taking-media-to-the-edge-harnessing-the-power-of-edge/index.astro";
const $$url$44 = "/resources/blog/taking-media-to-the-edge-harnessing-the-power-of-edge";

const index$44 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$44,
   file: $$file$44,
   url: $$url$44
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$43 = createAstro();
const $$Index$43 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$43, $$props, $$slots);
  Astro2.self = $$Index$43;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/5-tips-for-choosing-the-right-cdn-for-video-delivery/index.astro");

const $$file$43 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/5-tips-for-choosing-the-right-cdn-for-video-delivery/index.astro";
const $$url$43 = "/resources/blog/5-tips-for-choosing-the-right-cdn-for-video-delivery";

const index$43 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$43,
   file: $$file$43,
   url: $$url$43
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$42 = createAstro();
const $$Index$42 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$42, $$props, $$slots);
  Astro2.self = $$Index$42;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/download-delivery-advantages-with-the-edgio-platform/index.astro");

const $$file$42 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/download-delivery-advantages-with-the-edgio-platform/index.astro";
const $$url$42 = "/resources/blog/download-delivery-advantages-with-the-edgio-platform";

const index$42 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$42,
   file: $$file$42,
   url: $$url$42
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$41 = createAstro();
const $$Index$41 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$41, $$props, $$slots);
  Astro2.self = $$Index$41;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/the-secret-behind-blazingly-fast-shopify-plus-stores/index.astro");

const $$file$41 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/the-secret-behind-blazingly-fast-shopify-plus-stores/index.astro";
const $$url$41 = "/resources/blog/the-secret-behind-blazingly-fast-shopify-plus-stores";

const index$41 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$41,
   file: $$file$41,
   url: $$url$41
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$40 = createAstro();
const $$Index$40 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$40, $$props, $$slots);
  Astro2.self = $$Index$40;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/why-the-future-of-digital-experiences-is-at-the-edge/index.astro");

const $$file$40 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/why-the-future-of-digital-experiences-is-at-the-edge/index.astro";
const $$url$40 = "/resources/blog/why-the-future-of-digital-experiences-is-at-the-edge";

const index$40 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$40,
   file: $$file$40,
   url: $$url$40
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3$ = createAstro();
const $$Index$3$ = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3$, $$props, $$slots);
  Astro2.self = $$Index$3$;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/getting-real-on-cyber-security-with-darrin-reynolds/index.astro");

const $$file$3$ = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/getting-real-on-cyber-security-with-darrin-reynolds/index.astro";
const $$url$3$ = "/resources/blog/getting-real-on-cyber-security-with-darrin-reynolds";

const index$3$ = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3$,
   file: $$file$3$,
   url: $$url$3$
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3_ = createAstro();
const $$Index$3_ = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3_, $$props, $$slots);
  Astro2.self = $$Index$3_;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-we-deliver-a-seamless-cdn-onboarding-experience/index.astro");

const $$file$3_ = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-we-deliver-a-seamless-cdn-onboarding-experience/index.astro";
const $$url$3_ = "/resources/blog/how-we-deliver-a-seamless-cdn-onboarding-experience";

const index$3_ = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3_,
   file: $$file$3_,
   url: $$url$3_
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3Z = createAstro();
const $$Index$3Z = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3Z, $$props, $$slots);
  Astro2.self = $$Index$3Z;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/the-truth-about-guest-checkout-a-surprising-finding/index.astro");

const $$file$3Z = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/the-truth-about-guest-checkout-a-surprising-finding/index.astro";
const $$url$3Z = "/resources/blog/the-truth-about-guest-checkout-a-surprising-finding";

const index$3Z = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3Z,
   file: $$file$3Z,
   url: $$url$3Z
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3Y = createAstro();
const $$Index$3Y = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3Y, $$props, $$slots);
  Astro2.self = $$Index$3Y;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edge-platforms-deliver-three-key-business-benefits/index.astro");

const $$file$3Y = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edge-platforms-deliver-three-key-business-benefits/index.astro";
const $$url$3Y = "/resources/blog/edge-platforms-deliver-three-key-business-benefits";

const index$3Y = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3Y,
   file: $$file$3Y,
   url: $$url$3Y
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3X = createAstro();
const $$Index$3X = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3X, $$props, $$slots);
  Astro2.self = $$Index$3X;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/five-innovations-that-improve-live-event-streaming/index.astro");

const $$file$3X = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/five-innovations-that-improve-live-event-streaming/index.astro";
const $$url$3X = "/resources/blog/five-innovations-that-improve-live-event-streaming";

const index$3X = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3X,
   file: $$file$3X,
   url: $$url$3X
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3W = createAstro();
const $$Index$3W = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3W, $$props, $$slots);
  Astro2.self = $$Index$3W;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/limelight-completes-acquisition-of-yahoos-edgecast/index.astro");

const $$file$3W = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/limelight-completes-acquisition-of-yahoos-edgecast/index.astro";
const $$url$3W = "/resources/blog/limelight-completes-acquisition-of-yahoos-edgecast";

const index$3W = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3W,
   file: $$file$3W,
   url: $$url$3W
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3V = createAstro();
const $$Index$3V = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3V, $$props, $$slots);
  Astro2.self = $$Index$3V;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/limelight-networks-completes-acquisition-of-layer0/index.astro");

const $$file$3V = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/limelight-networks-completes-acquisition-of-layer0/index.astro";
const $$url$3V = "/resources/blog/limelight-networks-completes-acquisition-of-layer0";

const index$3V = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3V,
   file: $$file$3V,
   url: $$url$3V
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3U = createAstro();
const $$Index$3U = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3U, $$props, $$slots);
  Astro2.self = $$Index$3U;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-googles-speed-measurement-tools-penalize-spas/index.astro");

const $$file$3U = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-googles-speed-measurement-tools-penalize-spas/index.astro";
const $$url$3U = "/resources/blog/how-googles-speed-measurement-tools-penalize-spas";

const index$3U = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3U,
   file: $$file$3U,
   url: $$url$3U
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3T = createAstro();
const $$Index$3T = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3T, $$props, $$slots);
  Astro2.self = $$Index$3T;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/limelight-sets-new-online-traffic-delivery-record/index.astro");

const $$file$3T = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/limelight-sets-new-online-traffic-delivery-record/index.astro";
const $$url$3T = "/resources/blog/limelight-sets-new-online-traffic-delivery-record";

const index$3T = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3T,
   file: $$file$3T,
   url: $$url$3T
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3S = createAstro();
const $$Index$3S = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3S, $$props, $$slots);
  Astro2.self = $$Index$3S;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/nuxtshop-a-nuxt-and-shopify-ecommerce-starter-kit/index.astro");

const $$file$3S = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/nuxtshop-a-nuxt-and-shopify-ecommerce-starter-kit/index.astro";
const $$url$3S = "/resources/blog/nuxtshop-a-nuxt-and-shopify-ecommerce-starter-kit";

const index$3S = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3S,
   file: $$file$3S,
   url: $$url$3S
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3R = createAstro();
const $$Index$3R = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3R, $$props, $$slots);
  Astro2.self = $$Index$3R;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/whats-keeping-web-security-leaders-awake-at-night/index.astro");

const $$file$3R = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/whats-keeping-web-security-leaders-awake-at-night/index.astro";
const $$url$3R = "/resources/blog/whats-keeping-web-security-leaders-awake-at-night";

const index$3R = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3R,
   file: $$file$3R,
   url: $$url$3R
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3Q = createAstro();
const $$Index$3Q = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3Q, $$props, $$slots);
  Astro2.self = $$Index$3Q;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/five-security-questions-every-developer-must-ask/index.astro");

const $$file$3Q = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/five-security-questions-every-developer-must-ask/index.astro";
const $$url$3Q = "/resources/blog/five-security-questions-every-developer-must-ask";

const index$3Q = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3Q,
   file: $$file$3Q,
   url: $$url$3Q
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3P = createAstro();
const $$Index$3P = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3P, $$props, $$slots);
  Astro2.self = $$Index$3P;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/serverless-computing-for-real-world-applications/index.astro");

const $$file$3P = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/serverless-computing-for-real-world-applications/index.astro";
const $$url$3P = "/resources/blog/serverless-computing-for-real-world-applications";

const index$3P = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3P,
   file: $$file$3P,
   url: $$url$3P
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3O = createAstro();
const $$Index$3O = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3O, $$props, $$slots);
  Astro2.self = $$Index$3O;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/what-does-it-mean-to-be-client-obsessed-at-edgio/index.astro");

const $$file$3O = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/what-does-it-mean-to-be-client-obsessed-at-edgio/index.astro";
const $$url$3O = "/resources/blog/what-does-it-mean-to-be-client-obsessed-at-edgio";

const index$3O = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3O,
   file: $$file$3O,
   url: $$url$3O
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3N = createAstro();
const $$Index$3N = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3N, $$props, $$slots);
  Astro2.self = $$Index$3N;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-edge-compute-transforms-digital-experiences/index.astro");

const $$file$3N = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-edge-compute-transforms-digital-experiences/index.astro";
const $$url$3N = "/resources/blog/how-edge-compute-transforms-digital-experiences";

const index$3N = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3N,
   file: $$file$3N,
   url: $$url$3N
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3M = createAstro();
const $$Index$3M = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3M, $$props, $$slots);
  Astro2.self = $$Index$3M;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/impact-of-adaptive-load-balancing-on-production/index.astro");

const $$file$3M = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/impact-of-adaptive-load-balancing-on-production/index.astro";
const $$url$3M = "/resources/blog/impact-of-adaptive-load-balancing-on-production";

const index$3M = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3M,
   file: $$file$3M,
   url: $$url$3M
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3L = createAstro();
const $$Index$3L = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3L, $$props, $$slots);
  Astro2.self = $$Index$3L;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/increase-customer-protection-with-edge-security/index.astro");

const $$file$3L = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/increase-customer-protection-with-edge-security/index.astro";
const $$url$3L = "/resources/blog/increase-customer-protection-with-edge-security";

const index$3L = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3L,
   file: $$file$3L,
   url: $$url$3L
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3K = createAstro();
const $$Index$3K = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3K, $$props, $$slots);
  Astro2.self = $$Index$3K;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/limelight-reports-record-fourth-quarter-results/index.astro");

const $$file$3K = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/limelight-reports-record-fourth-quarter-results/index.astro";
const $$url$3K = "/resources/blog/limelight-reports-record-fourth-quarter-results";

const index$3K = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3K,
   file: $$file$3K,
   url: $$url$3K
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3J = createAstro();
const $$Index$3J = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3J, $$props, $$slots);
  Astro2.self = $$Index$3J;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/celebrating-edgio-engineers-katherine-giardina/index.astro");

const $$file$3J = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/celebrating-edgio-engineers-katherine-giardina/index.astro";
const $$url$3J = "/resources/blog/celebrating-edgio-engineers-katherine-giardina";

const index$3J = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3J,
   file: $$file$3J,
   url: $$url$3J
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3I = createAstro();
const $$Index$3I = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3I, $$props, $$slots);
  Astro2.self = $$Index$3I;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edgio-celebrates-international-womens-day-2023/index.astro");

const $$file$3I = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edgio-celebrates-international-womens-day-2023/index.astro";
const $$url$3I = "/resources/blog/edgio-celebrates-international-womens-day-2023";

const index$3I = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3I,
   file: $$file$3I,
   url: $$url$3I
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3H = createAstro();
const $$Index$3H = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3H, $$props, $$slots);
  Astro2.self = $$Index$3H;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edgio-delivery-provides-support-for-websockets/index.astro");

const $$file$3H = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edgio-delivery-provides-support-for-websockets/index.astro";
const $$url$3H = "/resources/blog/edgio-delivery-provides-support-for-websockets";

const index$3H = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3H,
   file: $$file$3H,
   url: $$url$3H
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3G = createAstro();
const $$Index$3G = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3G, $$props, $$slots);
  Astro2.self = $$Index$3G;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/ensure-sub-second-load-for-your-ecommerce-site/index.astro");

const $$file$3G = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/ensure-sub-second-load-for-your-ecommerce-site/index.astro";
const $$url$3G = "/resources/blog/ensure-sub-second-load-for-your-ecommerce-site";

const index$3G = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3G,
   file: $$file$3G,
   url: $$url$3G
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3F = createAstro();
const $$Index$3F = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3F, $$props, $$slots);
  Astro2.self = $$Index$3F;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/serverless-deep-request-inspection-has-arrived/index.astro");

const $$file$3F = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/serverless-deep-request-inspection-has-arrived/index.astro";
const $$url$3F = "/resources/blog/serverless-deep-request-inspection-has-arrived";

const index$3F = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3F,
   file: $$file$3F,
   url: $$url$3F
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3E = createAstro();
const $$Index$3E = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3E, $$props, $$slots);
  Astro2.self = $$Index$3E;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/spotlight-on-first-team-leaders-charley-thomas/index.astro");

const $$file$3E = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/spotlight-on-first-team-leaders-charley-thomas/index.astro";
const $$url$3E = "/resources/blog/spotlight-on-first-team-leaders-charley-thomas";

const index$3E = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3E,
   file: $$file$3E,
   url: $$url$3E
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3D = createAstro();
const $$Index$3D = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3D, $$props, $$slots);
  Astro2.self = $$Index$3D;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/the-internet-is-faster-but-websites-are-slower/index.astro");

const $$file$3D = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/the-internet-is-faster-but-websites-are-slower/index.astro";
const $$url$3D = "/resources/blog/the-internet-is-faster-but-websites-are-slower";

const index$3D = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3D,
   file: $$file$3D,
   url: $$url$3D
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3C = createAstro();
const $$Index$3C = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3C, $$props, $$slots);
  Astro2.self = $$Index$3C;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/celebrating-edgio-engineers-cathleen-griffeth/index.astro");

const $$file$3C = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/celebrating-edgio-engineers-cathleen-griffeth/index.astro";
const $$url$3C = "/resources/blog/celebrating-edgio-engineers-cathleen-griffeth";

const index$3C = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3C,
   file: $$file$3C,
   url: $$url$3C
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3B = createAstro();
const $$Index$3B = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3B, $$props, $$slots);
  Astro2.self = $$Index$3B;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-last-mile-providers-can-gain-cdn-benefits/index.astro");

const $$file$3B = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-last-mile-providers-can-gain-cdn-benefits/index.astro";
const $$url$3B = "/resources/blog/how-last-mile-providers-can-gain-cdn-benefits";

const index$3B = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3B,
   file: $$file$3B,
   url: $$url$3B
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3A = createAstro();
const $$Index$3A = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3A, $$props, $$slots);
  Astro2.self = $$Index$3A;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/protecting-your-ott-service-from-ddos-attacks/index.astro");

const $$file$3A = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/protecting-your-ott-service-from-ddos-attacks/index.astro";
const $$url$3A = "/resources/blog/protecting-your-ott-service-from-ddos-attacks";

const index$3A = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3A,
   file: $$file$3A,
   url: $$url$3A
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3z = createAstro();
const $$Index$3z = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3z, $$props, $$slots);
  Astro2.self = $$Index$3z;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/spotlight-on-first-team-leaders-ginger-hudson/index.astro");

const $$file$3z = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/spotlight-on-first-team-leaders-ginger-hudson/index.astro";
const $$url$3z = "/resources/blog/spotlight-on-first-team-leaders-ginger-hudson";

const index$3z = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3z,
   file: $$file$3z,
   url: $$url$3z
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3y = createAstro();
const $$Index$3y = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3y, $$props, $$slots);
  Astro2.self = $$Index$3y;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/spotlight-on-first-team-leaders-jeff-williams/index.astro");

const $$file$3y = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/spotlight-on-first-team-leaders-jeff-williams/index.astro";
const $$url$3y = "/resources/blog/spotlight-on-first-team-leaders-jeff-williams";

const index$3y = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3y,
   file: $$file$3y,
   url: $$url$3y
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3x = createAstro();
const $$Index$3x = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3x, $$props, $$slots);
  Astro2.self = $$Index$3x;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edgio-waap-rules-creating-order-out-of-chaos/index.astro");

const $$file$3x = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edgio-waap-rules-creating-order-out-of-chaos/index.astro";
const $$url$3x = "/resources/blog/edgio-waap-rules-creating-order-out-of-chaos";

const index$3x = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3x,
   file: $$file$3x,
   url: $$url$3x
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3w = createAstro();
const $$Index$3w = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3w, $$props, $$slots);
  Astro2.self = $$Index$3w;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/whats-holding-back-your-web-builder-velocity/index.astro");

const $$file$3w = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/whats-holding-back-your-web-builder-velocity/index.astro";
const $$url$3w = "/resources/blog/whats-holding-back-your-web-builder-velocity";

const index$3w = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3w,
   file: $$file$3w,
   url: $$url$3w
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3v = createAstro();
const $$Index$3v = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3v, $$props, $$slots);
  Astro2.self = $$Index$3v;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/whats-new-in-version-6-of-edgio-applications/index.astro");

const $$file$3v = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/whats-new-in-version-6-of-edgio-applications/index.astro";
const $$url$3v = "/resources/blog/whats-new-in-version-6-of-edgio-applications";

const index$3v = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3v,
   file: $$file$3v,
   url: $$url$3v
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3u = createAstro();
const $$Index$3u = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3u, $$props, $$slots);
  Astro2.self = $$Index$3u;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/graphql-caching-edgejs-fiddle-astro-support/index.astro");

const $$file$3u = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/graphql-caching-edgejs-fiddle-astro-support/index.astro";
const $$url$3u = "/resources/blog/graphql-caching-edgejs-fiddle-astro-support";

const index$3u = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3u,
   file: $$file$3u,
   url: $$url$3u
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3t = createAstro();
const $$Index$3t = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3t, $$props, $$slots);
  Astro2.self = $$Index$3t;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/maximizing-content-value-with-edgio-control/index.astro");

const $$file$3t = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/maximizing-content-value-with-edgio-control/index.astro";
const $$url$3t = "/resources/blog/maximizing-content-value-with-edgio-control";

const index$3t = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3t,
   file: $$file$3t,
   url: $$url$3t
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3s = createAstro();
const $$Index$3s = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3s, $$props, $$slots);
  Astro2.self = $$Index$3s;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/optimizing-your-website-for-lighthouse-v6-0/index.astro");

const $$file$3s = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/optimizing-your-website-for-lighthouse-v6-0/index.astro";
const $$url$3s = "/resources/blog/optimizing-your-website-for-lighthouse-v6-0";

const index$3s = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3s,
   file: $$file$3s,
   url: $$url$3s
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3r = createAstro();
const $$Index$3r = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3r, $$props, $$slots);
  Astro2.self = $$Index$3r;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/real-time-core-web-vitals-and-rum-analytics/index.astro");

const $$file$3r = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/real-time-core-web-vitals-and-rum-analytics/index.astro";
const $$url$3r = "/resources/blog/real-time-core-web-vitals-and-rum-analytics";

const index$3r = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3r,
   file: $$file$3r,
   url: $$url$3r
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3q = createAstro();
const $$Index$3q = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3q, $$props, $$slots);
  Astro2.self = $$Index$3q;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/security-risk-in-ott-streaming-applications/index.astro");

const $$file$3q = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/security-risk-in-ott-streaming-applications/index.astro";
const $$url$3q = "/resources/blog/security-risk-in-ott-streaming-applications";

const index$3q = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3q,
   file: $$file$3q,
   url: $$url$3q
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3p = createAstro();
const $$Index$3p = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3p, $$props, $$slots);
  Astro2.self = $$Index$3p;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/spotlight-on-first-team-leaders-rob-wakerly/index.astro");

const $$file$3p = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/spotlight-on-first-team-leaders-rob-wakerly/index.astro";
const $$url$3p = "/resources/blog/spotlight-on-first-team-leaders-rob-wakerly";

const index$3p = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3p,
   file: $$file$3p,
   url: $$url$3p
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3o = createAstro();
const $$Index$3o = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3o, $$props, $$slots);
  Astro2.self = $$Index$3o;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/celebrating-edgio-engineers-david-arellano/index.astro");

const $$file$3o = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/celebrating-edgio-engineers-david-arellano/index.astro";
const $$url$3o = "/resources/blog/celebrating-edgio-engineers-david-arellano";

const index$3o = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3o,
   file: $$file$3o,
   url: $$url$3o
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3n = createAstro();
const $$Index$3n = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3n, $$props, $$slots);
  Astro2.self = $$Index$3n;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-to-pick-a-javascript-framework-in-2023/index.astro");

const $$file$3n = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-to-pick-a-javascript-framework-in-2023/index.astro";
const $$url$3n = "/resources/blog/how-to-pick-a-javascript-framework-in-2023";

const index$3n = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3n,
   file: $$file$3n,
   url: $$url$3n
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3m = createAstro();
const $$Index$3m = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3m, $$props, $$slots);
  Astro2.self = $$Index$3m;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/project-himalaya-when-two-networks-combine/index.astro");

const $$file$3m = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/project-himalaya-when-two-networks-combine/index.astro";
const $$url$3m = "/resources/blog/project-himalaya-when-two-networks-combine";

const index$3m = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3m,
   file: $$file$3m,
   url: $$url$3m
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3l = createAstro();
const $$Index$3l = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3l, $$props, $$slots);
  Astro2.self = $$Index$3l;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/capitalize-on-ultra-low-latency-streaming/index.astro");

const $$file$3l = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/capitalize-on-ultra-low-latency-streaming/index.astro";
const $$url$3l = "/resources/blog/capitalize-on-ultra-low-latency-streaming";

const index$3l = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3l,
   file: $$file$3l,
   url: $$url$3l
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3k = createAstro();
const $$Index$3k = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3k, $$props, $$slots);
  Astro2.self = $$Index$3k;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/celebrating-edgio-engineers-scott-bromley/index.astro");

const $$file$3k = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/celebrating-edgio-engineers-scott-bromley/index.astro";
const $$url$3k = "/resources/blog/celebrating-edgio-engineers-scott-bromley";

const index$3k = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3k,
   file: $$file$3k,
   url: $$url$3k
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3j = createAstro();
const $$Index$3j = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3j, $$props, $$slots);
  Astro2.self = $$Index$3j;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edge-computing-in-a-sea-of-multiple-edges/index.astro");

const $$file$3j = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edge-computing-in-a-sea-of-multiple-edges/index.astro";
const $$url$3j = "/resources/blog/edge-computing-in-a-sea-of-multiple-edges";

const index$3j = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3j,
   file: $$file$3j,
   url: $$url$3j
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3i = createAstro();
const $$Index$3i = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3i, $$props, $$slots);
  Astro2.self = $$Index$3i;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/guide-to-incremental-static-re-generation/index.astro");

const $$file$3i = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/guide-to-incremental-static-re-generation/index.astro";
const $$url$3i = "/resources/blog/guide-to-incremental-static-re-generation";

const index$3i = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3i,
   file: $$file$3i,
   url: $$url$3i
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3h = createAstro();
const $$Index$3h = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3h, $$props, $$slots);
  Astro2.self = $$Index$3h;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/spotlight-on-first-team-leaders-edwin-koh/index.astro");

const $$file$3h = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/spotlight-on-first-team-leaders-edwin-koh/index.astro";
const $$url$3h = "/resources/blog/spotlight-on-first-team-leaders-edwin-koh";

const index$3h = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3h,
   file: $$file$3h,
   url: $$url$3h
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3g = createAstro();
const $$Index$3g = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3g, $$props, $$slots);
  Astro2.self = $$Index$3g;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/celebrating-edgio-engineers-craig-fowler/index.astro");

const $$file$3g = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/celebrating-edgio-engineers-craig-fowler/index.astro";
const $$url$3g = "/resources/blog/celebrating-edgio-engineers-craig-fowler";

const index$3g = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3g,
   file: $$file$3g,
   url: $$url$3g
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3f = createAstro();
const $$Index$3f = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3f, $$props, $$slots);
  Astro2.self = $$Index$3f;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/celebrating-edgio-engineers-erica-ehnert/index.astro");

const $$file$3f = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/celebrating-edgio-engineers-erica-ehnert/index.astro";
const $$url$3f = "/resources/blog/celebrating-edgio-engineers-erica-ehnert";

const index$3f = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3f,
   file: $$file$3f,
   url: $$url$3f
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3e = createAstro();
const $$Index$3e = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3e, $$props, $$slots);
  Astro2.self = $$Index$3e;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/celebrating-edgio-engineers-shaun-kruger/index.astro");

const $$file$3e = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/celebrating-edgio-engineers-shaun-kruger/index.astro";
const $$url$3e = "/resources/blog/celebrating-edgio-engineers-shaun-kruger";

const index$3e = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3e,
   file: $$file$3e,
   url: $$url$3e
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3d = createAstro();
const $$Index$3d = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3d, $$props, $$slots);
  Astro2.self = $$Index$3d;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-to-measure-my-sites-core-web-vitals/index.astro");

const $$file$3d = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-to-measure-my-sites-core-web-vitals/index.astro";
const $$url$3d = "/resources/blog/how-to-measure-my-sites-core-web-vitals";

const index$3d = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3d,
   file: $$file$3d,
   url: $$url$3d
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3c = createAstro();
const $$Index$3c = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3c, $$props, $$slots);
  Astro2.self = $$Index$3c;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/advanced-advertising-solutions-for-ott/index.astro");

const $$file$3c = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/advanced-advertising-solutions-for-ott/index.astro";
const $$url$3c = "/resources/blog/advanced-advertising-solutions-for-ott";

const index$3c = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3c,
   file: $$file$3c,
   url: $$url$3c
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3b = createAstro();
const $$Index$3b = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3b, $$props, $$slots);
  Astro2.self = $$Index$3b;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/celebrating-edgio-engineers-amit-cohen/index.astro");

const $$file$3b = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/celebrating-edgio-engineers-amit-cohen/index.astro";
const $$url$3b = "/resources/blog/celebrating-edgio-engineers-amit-cohen";

const index$3b = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3b,
   file: $$file$3b,
   url: $$url$3b
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3a = createAstro();
const $$Index$3a = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3a, $$props, $$slots);
  Astro2.self = $$Index$3a;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/celebrating-edgio-engineers-nadiia-gut/index.astro");

const $$file$3a = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/celebrating-edgio-engineers-nadiia-gut/index.astro";
const $$url$3a = "/resources/blog/celebrating-edgio-engineers-nadiia-gut";

const index$3a = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3a,
   file: $$file$3a,
   url: $$url$3a
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$39 = createAstro();
const $$Index$39 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$39, $$props, $$slots);
  Astro2.self = $$Index$39;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edgios-open-edge-adopted-by-ntt-docomo/index.astro");

const $$file$39 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edgios-open-edge-adopted-by-ntt-docomo/index.astro";
const $$url$39 = "/resources/blog/edgios-open-edge-adopted-by-ntt-docomo";

const index$39 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$39,
   file: $$file$39,
   url: $$url$39
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$38 = createAstro();
const $$Index$38 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$38, $$props, $$slots);
  Astro2.self = $$Index$38;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/prevent-online-traffic-jams-with-a-cdn/index.astro");

const $$file$38 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/prevent-online-traffic-jams-with-a-cdn/index.astro";
const $$url$38 = "/resources/blog/prevent-online-traffic-jams-with-a-cdn";

const index$38 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$38,
   file: $$file$38,
   url: $$url$38
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$37 = createAstro();
const $$Index$37 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$37, $$props, $$slots);
  Astro2.self = $$Index$37;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/waf-insights-web-security-for-everyone/index.astro");

const $$file$37 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/waf-insights-web-security-for-everyone/index.astro";
const $$url$37 = "/resources/blog/waf-insights-web-security-for-everyone";

const index$37 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$37,
   file: $$file$37,
   url: $$url$37
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$36 = createAstro();
const $$Index$36 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$36, $$props, $$slots);
  Astro2.self = $$Index$36;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/easily-optimizing-your-cache-hit-rate/index.astro");

const $$file$36 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/easily-optimizing-your-cache-hit-rate/index.astro";
const $$url$36 = "/resources/blog/easily-optimizing-your-cache-hit-rate";

const index$36 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$36,
   file: $$file$36,
   url: $$url$36
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$35 = createAstro();
const $$Index$35 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$35, $$props, $$slots);
  Astro2.self = $$Index$35;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/forensic-watermarking-at-the-cdn-edge/index.astro");

const $$file$35 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/forensic-watermarking-at-the-cdn-edge/index.astro";
const $$url$35 = "/resources/blog/forensic-watermarking-at-the-cdn-edge";

const index$35 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$35,
   file: $$file$35,
   url: $$url$35
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$34 = createAstro();
const $$Index$34 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$34, $$props, $$slots);
  Astro2.self = $$Index$34;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/limelight-to-acquire-yahoos-edgecast/index.astro");

const $$file$34 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/limelight-to-acquire-yahoos-edgecast/index.astro";
const $$url$34 = "/resources/blog/limelight-to-acquire-yahoos-edgecast";

const index$34 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$34,
   file: $$file$34,
   url: $$url$34
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$33 = createAstro();
const $$Index$33 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$33, $$props, $$slots);
  Astro2.self = $$Index$33;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/new-security-platform-from-limelight/index.astro");

const $$file$33 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/new-security-platform-from-limelight/index.astro";
const $$url$33 = "/resources/blog/new-security-platform-from-limelight";

const index$33 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$33,
   file: $$file$33,
   url: $$url$33
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$32 = createAstro();
const $$Index$32 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$32, $$props, $$slots);
  Astro2.self = $$Index$32;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/openssl-3x-high-vulnerability-patch/index.astro");

const $$file$32 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/openssl-3x-high-vulnerability-patch/index.astro";
const $$url$32 = "/resources/blog/openssl-3x-high-vulnerability-patch";

const index$32 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$32,
   file: $$file$32,
   url: $$url$32
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$31 = createAstro();
const $$Index$31 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$31, $$props, $$slots);
  Astro2.self = $$Index$31;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edgio-prevents-massive-ddos-attack/index.astro");

const $$file$31 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edgio-prevents-massive-ddos-attack/index.astro";
const $$url$31 = "/resources/blog/edgio-prevents-massive-ddos-attack";

const index$31 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$31,
   file: $$file$31,
   url: $$url$31
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$30 = createAstro();
const $$Index$30 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$30, $$props, $$slots);
  Astro2.self = $$Index$30;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/in-retrospect-the-journey-to-edgio/index.astro");

const $$file$30 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/in-retrospect-the-journey-to-edgio/index.astro";
const $$url$30 = "/resources/blog/in-retrospect-the-journey-to-edgio";

const index$30 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$30,
   file: $$file$30,
   url: $$url$30
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2$ = createAstro();
const $$Index$2$ = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2$, $$props, $$slots);
  Astro2.self = $$Index$2$;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/why-i-joined-edgio-stephen-cumming/index.astro");

const $$file$2$ = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/why-i-joined-edgio-stephen-cumming/index.astro";
const $$url$2$ = "/resources/blog/why-i-joined-edgio-stephen-cumming";

const index$2$ = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2$,
   file: $$file$2$,
   url: $$url$2$
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2_ = createAstro();
const $$Index$2_ = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2_, $$props, $$slots);
  Astro2.self = $$Index$2_;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/why-your-ecommerce-pwa-needs-a-bff/index.astro");

const $$file$2_ = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/why-your-ecommerce-pwa-needs-a-bff/index.astro";
const $$url$2_ = "/resources/blog/why-your-ecommerce-pwa-needs-a-bff";

const index$2_ = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2_,
   file: $$file$2_,
   url: $$url$2_
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2Z = createAstro();
const $$Index$2Z = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2Z, $$props, $$slots);
  Astro2.self = $$Index$2Z;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/celebrating-redwoodjs-launch-week/index.astro");

const $$file$2Z = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/celebrating-redwoodjs-launch-week/index.astro";
const $$url$2Z = "/resources/blog/celebrating-redwoodjs-launch-week";

const index$2Z = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2Z,
   file: $$file$2Z,
   url: $$url$2Z
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2Y = createAstro();
const $$Index$2Y = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2Y, $$props, $$slots);
  Astro2.self = $$Index$2Y;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/diagnose-serverless-memory-issues/index.astro");

const $$file$2Y = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/diagnose-serverless-memory-issues/index.astro";
const $$url$2Y = "/resources/blog/diagnose-serverless-memory-issues";

const index$2Y = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2Y,
   file: $$file$2Y,
   url: $$url$2Y
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2X = createAstro();
const $$Index$2X = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2X, $$props, $$slots);
  Astro2.self = $$Index$2X;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/fastest-salesforce-commerce-cloud/index.astro");

const $$file$2X = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/fastest-salesforce-commerce-cloud/index.astro";
const $$url$2X = "/resources/blog/fastest-salesforce-commerce-cloud";

const index$2X = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2X,
   file: $$file$2X,
   url: $$url$2X
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2W = createAstro();
const $$Index$2W = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2W, $$props, $$slots);
  Astro2.self = $$Index$2W;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/implementing-a-multi-cdn-strategy/index.astro");

const $$file$2W = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/implementing-a-multi-cdn-strategy/index.astro";
const $$url$2W = "/resources/blog/implementing-a-multi-cdn-strategy";

const index$2W = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2W,
   file: $$file$2W,
   url: $$url$2W
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2V = createAstro();
const $$Index$2V = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2V, $$props, $$slots);
  Astro2.self = $$Index$2V;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/episode-ix-the-rise-of-streaming/index.astro");

const $$file$2V = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/episode-ix-the-rise-of-streaming/index.astro";
const $$url$2V = "/resources/blog/episode-ix-the-rise-of-streaming";

const index$2V = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2V,
   file: $$file$2V,
   url: $$url$2V
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2U = createAstro();
const $$Index$2U = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2U, $$props, $$slots);
  Astro2.self = $$Index$2U;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/live-sports-get-back-to-business/index.astro");

const $$file$2U = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/live-sports-get-back-to-business/index.astro";
const $$url$2U = "/resources/blog/live-sports-get-back-to-business";

const index$2U = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2U,
   file: $$file$2U,
   url: $$url$2U
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2T = createAstro();
const $$Index$2T = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2T, $$props, $$slots);
  Astro2.self = $$Index$2T;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/promise-of-the-edge-cloud-and-5G/index.astro");

const $$file$2T = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/promise-of-the-edge-cloud-and-5G/index.astro";
const $$url$2T = "/resources/blog/promise-of-the-edge-cloud-and-5G";

const index$2T = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2T,
   file: $$file$2T,
   url: $$url$2T
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2S = createAstro();
const $$Index$2S = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2S, $$props, $$slots);
  Astro2.self = $$Index$2S;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/what-5G-means-for-realtime-video/index.astro");

const $$file$2S = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/what-5G-means-for-realtime-video/index.astro";
const $$url$2S = "/resources/blog/what-5G-means-for-realtime-video";

const index$2S = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2S,
   file: $$file$2S,
   url: $$url$2S
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2R = createAstro();
const $$Index$2R = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2R, $$props, $$slots);
  Astro2.self = $$Index$2R;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/dan-rayburn-edgecast-final-sale/index.astro");

const $$file$2R = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/dan-rayburn-edgecast-final-sale/index.astro";
const $$url$2R = "/resources/blog/dan-rayburn-edgecast-final-sale";

const index$2R = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2R,
   file: $$file$2R,
   url: $$url$2R
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2Q = createAstro();
const $$Index$2Q = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2Q, $$props, $$slots);
  Astro2.self = $$Index$2Q;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/jamstack-for-ecommerce-at-scale/index.astro");

const $$file$2Q = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/jamstack-for-ecommerce-at-scale/index.astro";
const $$url$2Q = "/resources/blog/jamstack-for-ecommerce-at-scale";

const index$2Q = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2Q,
   file: $$file$2Q,
   url: $$url$2Q
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2P = createAstro();
const $$Index$2P = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2P, $$props, $$slots);
  Astro2.self = $$Index$2P;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/live-sports-streaming-providers/index.astro");

const $$file$2P = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/live-sports-streaming-providers/index.astro";
const $$url$2P = "/resources/blog/live-sports-streaming-providers";

const index$2P = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2P,
   file: $$file$2P,
   url: $$url$2P
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2O = createAstro();
const $$Index$2O = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2O, $$props, $$slots);
  Astro2.self = $$Index$2O;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/ready-to-support-your-multi-cdn/index.astro");

const $$file$2O = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/ready-to-support-your-multi-cdn/index.astro";
const $$url$2O = "/resources/blog/ready-to-support-your-multi-cdn";

const index$2O = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2O,
   file: $$file$2O,
   url: $$url$2O
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2N = createAstro();
const $$Index$2N = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2N, $$props, $$slots);
  Astro2.self = $$Index$2N;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/akamai-vs-cloudflare-vs-layer0/index.astro");

const $$file$2N = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/akamai-vs-cloudflare-vs-layer0/index.astro";
const $$url$2N = "/resources/blog/akamai-vs-cloudflare-vs-layer0";

const index$2N = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2N,
   file: $$file$2N,
   url: $$url$2N
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2M = createAstro();
const $$Index$2M = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2M, $$props, $$slots);
  Astro2.self = $$Index$2M;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/announcing-layer0-by-limelight/index.astro");

const $$file$2M = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/announcing-layer0-by-limelight/index.astro";
const $$url$2M = "/resources/blog/announcing-layer0-by-limelight";

const index$2M = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2M,
   file: $$file$2M,
   url: $$url$2M
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2L = createAstro();
const $$Index$2L = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2L, $$props, $$slots);
  Astro2.self = $$Index$2L;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/ed-tech-comes-of-age-overnight/index.astro");

const $$file$2L = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/ed-tech-comes-of-age-overnight/index.astro";
const $$url$2L = "/resources/blog/ed-tech-comes-of-age-overnight";

const index$2L = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2L,
   file: $$file$2L,
   url: $$url$2L
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2K = createAstro();
const $$Index$2K = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2K, $$props, $$slots);
  Astro2.self = $$Index$2K;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/gen-z-the-global-game-changers/index.astro");

const $$file$2K = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/gen-z-the-global-game-changers/index.astro";
const $$url$2K = "/resources/blog/gen-z-the-global-game-changers";

const index$2K = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2K,
   file: $$file$2K,
   url: $$url$2K
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2J = createAstro();
const $$Index$2J = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2J, $$props, $$slots);
  Astro2.self = $$Index$2J;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-to-fix-your-broken-web-cdn/index.astro");

const $$file$2J = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/how-to-fix-your-broken-web-cdn/index.astro";
const $$url$2J = "/resources/blog/how-to-fix-your-broken-web-cdn";

const index$2J = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2J,
   file: $$file$2J,
   url: $$url$2J
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2I = createAstro();
const $$Index$2I = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2I, $$props, $$slots);
  Astro2.self = $$Index$2I;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/more-profitable-ott-businesses/index.astro");

const $$file$2I = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/more-profitable-ott-businesses/index.astro";
const $$url$2I = "/resources/blog/more-profitable-ott-businesses";

const index$2I = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2I,
   file: $$file$2I,
   url: $$url$2I
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2H = createAstro();
const $$Index$2H = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2H, $$props, $$slots);
  Astro2.self = $$Index$2H;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/why-i-am-a-strong-buy-on-edgio/index.astro");

const $$file$2H = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/why-i-am-a-strong-buy-on-edgio/index.astro";
const $$url$2H = "/resources/blog/why-i-am-a-strong-buy-on-edgio";

const index$2H = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2H,
   file: $$file$2H,
   url: $$url$2H
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2G = createAstro();
const $$Index$2G = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2G, $$props, $$slots);
  Astro2.self = $$Index$2G;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/optimize-your-video-workflow/index.astro");

const $$file$2G = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/optimize-your-video-workflow/index.astro";
const $$url$2G = "/resources/blog/optimize-your-video-workflow";

const index$2G = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2G,
   file: $$file$2G,
   url: $$url$2G
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2F = createAstro();
const $$Index$2F = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2F, $$props, $$slots);
  Astro2.self = $$Index$2F;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/exploring-edge-ai-use-cases/index.astro");

const $$file$2F = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/exploring-edge-ai-use-cases/index.astro";
const $$url$2F = "/resources/blog/exploring-edge-ai-use-cases";

const index$2F = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2F,
   file: $$file$2F,
   url: $$url$2F
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2E = createAstro();
const $$Index$2E = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2E, $$props, $$slots);
  Astro2.self = $$Index$2E;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/state-of-online-gaming-2021/index.astro");

const $$file$2E = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/state-of-online-gaming-2021/index.astro";
const $$url$2E = "/resources/blog/state-of-online-gaming-2021";

const index$2E = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2E,
   file: $$file$2E,
   url: $$url$2E
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2D = createAstro();
const $$Index$2D = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2D, $$props, $$slots);
  Astro2.self = $$Index$2D;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/wordpress-support-on-layer0/index.astro");

const $$file$2D = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/wordpress-support-on-layer0/index.astro";
const $$url$2D = "/resources/blog/wordpress-support-on-layer0";

const index$2D = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2D,
   file: $$file$2D,
   url: $$url$2D
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2C = createAstro();
const $$Index$2C = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2C, $$props, $$slots);
  Astro2.self = $$Index$2C;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/theres-more-to-tdd-than-qa/index.astro");

const $$file$2C = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/theres-more-to-tdd-than-qa/index.astro";
const $$url$2C = "/resources/blog/theres-more-to-tdd-than-qa";

const index$2C = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2C,
   file: $$file$2C,
   url: $$url$2C
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2B = createAstro();
const $$Index$2B = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2B, $$props, $$slots);
  Astro2.self = $$Index$2B;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/waf-management-made-simple/index.astro");

const $$file$2B = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/waf-management-made-simple/index.astro";
const $$url$2B = "/resources/blog/waf-management-made-simple";

const index$2B = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2B,
   file: $$file$2B,
   url: $$url$2B
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2A = createAstro();
const $$Index$2A = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2A, $$props, $$slots);
  Astro2.self = $$Index$2A;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edge-ai-why-we-built-it/index.astro");

const $$file$2A = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edge-ai-why-we-built-it/index.astro";
const $$url$2A = "/resources/blog/edge-ai-why-we-built-it";

const index$2A = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2A,
   file: $$file$2A,
   url: $$url$2A
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2z = createAstro();
const $$Index$2z = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2z, $$props, $$slots);
  Astro2.self = $$Index$2z;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edge-compute-panel-2022/index.astro");

const $$file$2z = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edge-compute-panel-2022/index.astro";
const $$url$2z = "/resources/blog/edge-compute-panel-2022";

const index$2z = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2z,
   file: $$file$2z,
   url: $$url$2z
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2y = createAstro();
const $$Index$2y = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2y, $$props, $$slots);
  Astro2.self = $$Index$2y;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/the-core-of-web-vitals/index.astro");

const $$file$2y = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/the-core-of-web-vitals/index.astro";
const $$url$2y = "/resources/blog/the-core-of-web-vitals";

const index$2y = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2y,
   file: $$file$2y,
   url: $$url$2y
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2x = createAstro();
const $$Index$2x = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2x, $$props, $$slots);
  Astro2.self = $$Index$2x;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/layer0-cdn-comparison/index.astro");

const $$file$2x = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/layer0-cdn-comparison/index.astro";
const $$url$2x = "/resources/blog/layer0-cdn-comparison";

const index$2x = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2x,
   file: $$file$2x,
   url: $$url$2x
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2w = createAstro();
const $$Index$2w = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2w, $$props, $$slots);
  Astro2.self = $$Index$2w;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edge-ai-how-it-works/index.astro");

const $$file$2w = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/edge-ai-how-it-works/index.astro";
const $$url$2w = "/resources/blog/edge-ai-how-it-works";

const index$2w = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2w,
   file: $$file$2w,
   url: $$url$2w
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2v = createAstro();
const $$Index$2v = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2v, $$props, $$slots);
  Astro2.self = $$Index$2v;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/waf-tuning-made-easy/index.astro");

const $$file$2v = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/waf-tuning-made-easy/index.astro";
const $$url$2v = "/resources/blog/waf-tuning-made-easy";

const index$2v = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2v,
   file: $$file$2v,
   url: $$url$2v
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2u = createAstro();
const $$Index$2u = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2u, $$props, $$slots);
  Astro2.self = $$Index$2u;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/we-are-finally-edgio/index.astro");

const $$file$2u = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/we-are-finally-edgio/index.astro";
const $$url$2u = "/resources/blog/we-are-finally-edgio";

const index$2u = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2u,
   file: $$file$2u,
   url: $$url$2u
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2t = createAstro();
const $$Index$2t = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2t, $$props, $$slots);
  Astro2.self = $$Index$2t;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/credential-stuffing/index.astro");

const $$file$2t = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/credential-stuffing/index.astro";
const $$url$2t = "/resources/blog/credential-stuffing";

const index$2t = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2t,
   file: $$file$2t,
   url: $$url$2t
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2s = createAstro();
const $$Index$2s = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2s, $$props, $$slots);
  Astro2.self = $$Index$2s;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/migration-made-easy/index.astro");

const $$file$2s = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/migration-made-easy/index.astro";
const $$url$2s = "/resources/blog/migration-made-easy";

const index$2s = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2s,
   file: $$file$2s,
   url: $$url$2s
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2r = createAstro();
const $$Index$2r = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2r, $$props, $$slots);
  Astro2.self = $$Index$2r;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/becoming-edgio/index.astro");

const $$file$2r = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/resources/blog/becoming-edgio/index.astro";
const $$url$2r = "/resources/blog/becoming-edgio";

const index$2r = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2r,
   file: $$file$2r,
   url: $$url$2r
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2q = createAstro();
const $$Index$2q = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2q, $$props, $$slots);
  Astro2.self = $$Index$2q;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/solutions/protect-websites-and-applications/index.astro");

const $$file$2q = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/solutions/protect-websites-and-applications/index.astro";
const $$url$2q = "/solutions/protect-websites-and-applications";

const index$2q = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2q,
   file: $$file$2q,
   url: $$url$2q
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2p = createAstro();
const $$Index$2p = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2p, $$props, $$slots);
  Astro2.self = $$Index$2p;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/solutions/maximizing-website-performance/index.astro");

const $$file$2p = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/solutions/maximizing-website-performance/index.astro";
const $$url$2p = "/solutions/maximizing-website-performance";

const index$2p = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2p,
   file: $$file$2p,
   url: $$url$2p
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2o = createAstro();
const $$Index$2o = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2o, $$props, $$slots);
  Astro2.self = $$Index$2o;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/solutions/broadcast-transformation/index.astro");

const $$file$2o = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/solutions/broadcast-transformation/index.astro";
const $$url$2o = "/solutions/broadcast-transformation";

const index$2o = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2o,
   file: $$file$2o,
   url: $$url$2o
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2n = createAstro();
const $$Index$2n = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2n, $$props, $$slots);
  Astro2.self = $$Index$2n;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/solutions/developer-team-velocity/index.astro");

const $$file$2n = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/solutions/developer-team-velocity/index.astro";
const $$url$2n = "/solutions/developer-team-velocity";

const index$2n = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2n,
   file: $$file$2n,
   url: $$url$2n
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2m = createAstro();
const $$Index$2m = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2m, $$props, $$slots);
  Astro2.self = $$Index$2m;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/solutions/media-and-entertainment/index.astro");

const $$file$2m = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/solutions/media-and-entertainment/index.astro";
const $$url$2m = "/solutions/media-and-entertainment";

const index$2m = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2m,
   file: $$file$2m,
   url: $$url$2m
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2l = createAstro();
const $$Index$2l = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2l, $$props, $$slots);
  Astro2.self = $$Index$2l;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/solutions/live-streaming/index.astro");

const $$file$2l = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/solutions/live-streaming/index.astro";
const $$url$2l = "/solutions/live-streaming";

const index$2l = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2l,
   file: $$file$2l,
   url: $$url$2l
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2k = createAstro();
const $$Index$2k = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2k, $$props, $$slots);
  Astro2.self = $$Index$2k;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/get-demo/index.astro");

const $$file$2k = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/get-demo/index.astro";
const $$url$2k = "/get-demo";

const index$2k = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2k,
   file: $$file$2k,
   url: $$url$2k
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2j = createAstro();
const $$Index$2j = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2j, $$props, $$slots);
  Astro2.self = $$Index$2j;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/services/index.astro");

const $$file$2j = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/services/index.astro";
const $$url$2j = "/services";

const index$2j = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2j,
   file: $$file$2j,
   url: $$url$2j
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2i = createAstro();
const $$Index$2i = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2i, $$props, $$slots);
  Astro2.self = $$Index$2i;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/company/leadership/index.astro");

const $$file$2i = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/company/leadership/index.astro";
const $$url$2i = "/company/leadership";

const index$2i = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2i,
   file: $$file$2i,
   url: $$url$2i
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2h = createAstro();
const $$Index$2h = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2h, $$props, $$slots);
  Astro2.self = $$Index$2h;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/company/about-us/index.astro");

const $$file$2h = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/company/about-us/index.astro";
const $$url$2h = "/company/about-us";

const index$2h = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2h,
   file: $$file$2h,
   url: $$url$2h
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2g = createAstro();
const $$Index$2g = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2g, $$props, $$slots);
  Astro2.self = $$Index$2g;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/company/careers/index.astro");

const $$file$2g = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/company/careers/index.astro";
const $$url$2g = "/company/careers";

const index$2g = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2g,
   file: $$file$2g,
   url: $$url$2g
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2f = createAstro();
const $$Index$2f = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2f, $$props, $$slots);
  Astro2.self = $$Index$2f;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/peering/index.astro");

const $$file$2f = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/peering/index.astro";
const $$url$2f = "/peering";

const index$2f = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2f,
   file: $$file$2f,
   url: $$url$2f
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2e = createAstro();
const $$Index$2e = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2e, $$props, $$slots);
  Astro2.self = $$Index$2e;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/legal/index.astro");

const $$file$2e = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/legal/index.astro";
const $$url$2e = "/legal";

const index$2e = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2e,
   file: $$file$2e,
   url: $$url$2e
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2d = createAstro();
const $$Index$2d = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2d, $$props, $$slots);
  Astro2.self = $$Index$2d;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/legal/disclaimer-copyright-disputes/index.astro");

const $$file$2d = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/legal/disclaimer-copyright-disputes/index.astro";
const $$url$2d = "/legal/disclaimer-copyright-disputes";

const index$2d = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2d,
   file: $$file$2d,
   url: $$url$2d
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2c = createAstro();
const $$Index$2c = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2c, $$props, $$slots);
  Astro2.self = $$Index$2c;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/legal/data-protection-addendum/index.astro");

const $$file$2c = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/legal/data-protection-addendum/index.astro";
const $$url$2c = "/legal/data-protection-addendum";

const index$2c = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2c,
   file: $$file$2c,
   url: $$url$2c
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2b = createAstro();
const $$Index$2b = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2b, $$props, $$slots);
  Astro2.self = $$Index$2b;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/legal/acceptable-use-policy/index.astro");

const $$file$2b = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/legal/acceptable-use-policy/index.astro";
const $$url$2b = "/legal/acceptable-use-policy";

const index$2b = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2b,
   file: $$file$2b,
   url: $$url$2b
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2a = createAstro();
const $$Index$2a = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2a, $$props, $$slots);
  Astro2.self = $$Index$2a;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/legal/invoice-methodologies/index.astro");

const $$file$2a = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/legal/invoice-methodologies/index.astro";
const $$url$2a = "/legal/invoice-methodologies";

const index$2a = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2a,
   file: $$file$2a,
   url: $$url$2a
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$29 = createAstro();
const $$Index$29 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$29, $$props, $$slots);
  Astro2.self = $$Index$29;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/legal/terms-of-service/index.astro");

const $$file$29 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/legal/terms-of-service/index.astro";
const $$url$29 = "/legal/terms-of-service";

const index$29 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$29,
   file: $$file$29,
   url: $$url$29
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$28 = createAstro();
const $$Index$28 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$28, $$props, $$slots);
  Astro2.self = $$Index$28;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/legal/privacy-policy/index.astro");

const $$file$28 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/legal/privacy-policy/index.astro";
const $$url$28 = "/legal/privacy-policy";

const index$28 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$28,
   file: $$file$28,
   url: $$url$28
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$27 = createAstro();
const $$Index$27 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$27, $$props, $$slots);
  Astro2.self = $$Index$27;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/legal/privacy-shield/index.astro");

const $$file$27 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/legal/privacy-shield/index.astro";
const $$url$27 = "/legal/privacy-shield";

const index$27 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$27,
   file: $$file$27,
   url: $$url$27
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$26 = createAstro();
const $$Index$26 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$26, $$props, $$slots);
  Astro2.self = $$Index$26;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/media/index.astro");

const $$file$26 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/media/index.astro";
const $$url$26 = "/media";

const index$26 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$26,
   file: $$file$26,
   url: $$url$26
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$25 = createAstro();
const $$Index$25 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$25, $$props, $$slots);
  Astro2.self = $$Index$25;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/media/open-edge/index.astro");

const $$file$25 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/media/open-edge/index.astro";
const $$url$25 = "/media/open-edge";

const index$25 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$25,
   file: $$file$25,
   url: $$url$25
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$24 = createAstro();
const $$Index$24 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$24, $$props, $$slots);
  Astro2.self = $$Index$24;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/media/delivery/index.astro");

const $$file$24 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/media/delivery/index.astro";
const $$url$24 = "/media/delivery";

const index$24 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$24,
   file: $$file$24,
   url: $$url$24
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$23 = createAstro();
const $$Index$23 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$23, $$props, $$slots);
  Astro2.self = $$Index$23;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/media/uplynk/index.astro");

const $$file$23 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/media/uplynk/index.astro";
const $$url$23 = "/media/uplynk";

const index$23 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$23,
   file: $$file$23,
   url: $$url$23
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$22 = createAstro();
const $$Index$22 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$22, $$props, $$slots);
  Astro2.self = $$Index$22;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/app/index.astro");

const $$file$22 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/app/index.astro";
const $$url$22 = "/app";

const index$22 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$22,
   file: $$file$22,
   url: $$url$22
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$21 = createAstro();
const $$Index$21 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$21, $$props, $$slots);
  Astro2.self = $$Index$21;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/app/performance/index.astro");

const $$file$21 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/app/performance/index.astro";
const $$url$21 = "/app/performance";

const index$21 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$21,
   file: $$file$21,
   url: $$url$21
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$20 = createAstro();
const $$Index$20 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$20, $$props, $$slots);
  Astro2.self = $$Index$20;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/app/security/index.astro");

const $$file$20 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/app/security/index.astro";
const $$url$20 = "/app/security";

const index$20 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$20,
   file: $$file$20,
   url: $$url$20
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1$ = createAstro();
const $$Index$1$ = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1$, $$props, $$slots);
  Astro2.self = $$Index$1$;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/app/security/advanced-bot-manager/index.astro");

const $$file$1$ = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/app/security/advanced-bot-manager/index.astro";
const $$url$1$ = "/app/security/advanced-bot-manager";

const index$1$ = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1$,
   file: $$file$1$,
   url: $$url$1$
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1_ = createAstro();
const $$Index$1_ = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1_, $$props, $$slots);
  Astro2.self = $$Index$1_;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/app/sites/index.astro");

const $$file$1_ = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/app/sites/index.astro";
const $$url$1_ = "/app/sites";

const index$1_ = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1_,
   file: $$file$1_,
   url: $$url$1_
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1Z = createAstro();
const $$Index$1Z = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1Z, $$props, $$slots);
  Astro2.self = $$Index$1Z;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/index.astro");

const $$file$1Z = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/index.astro";
const $$url$1Z = "/cn";

const index$1Z = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1Z,
   file: $$file$1Z,
   url: $$url$1Z
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1Y = createAstro();
const $$Index$1Y = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1Y, $$props, $$slots);
  Astro2.self = $$Index$1Y;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/resources/blog/edgio-earns-cyber-defense-global-infosec-award-at-rsa-2023/index.astro");

const $$file$1Y = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/resources/blog/edgio-earns-cyber-defense-global-infosec-award-at-rsa-2023/index.astro";
const $$url$1Y = "/cn/resources/blog/edgio-earns-cyber-defense-global-infosec-award-at-rsa-2023";

const index$1Y = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1Y,
   file: $$file$1Y,
   url: $$url$1Y
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1X = createAstro();
const $$Index$1X = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1X, $$props, $$slots);
  Astro2.self = $$Index$1X;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/resources/blog/future-of-cybersecurity-edgio-observations-from-rsa-2023/index.astro");

const $$file$1X = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/resources/blog/future-of-cybersecurity-edgio-observations-from-rsa-2023/index.astro";
const $$url$1X = "/cn/resources/blog/future-of-cybersecurity-edgio-observations-from-rsa-2023";

const index$1X = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1X,
   file: $$file$1X,
   url: $$url$1X
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1W = createAstro();
const $$Index$1W = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1W, $$props, $$slots);
  Astro2.self = $$Index$1W;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/resources/blog/edge-platforms-deliver-three-key-business-benefits/index.astro");

const $$file$1W = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/resources/blog/edge-platforms-deliver-three-key-business-benefits/index.astro";
const $$url$1W = "/cn/resources/blog/edge-platforms-deliver-three-key-business-benefits";

const index$1W = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1W,
   file: $$file$1W,
   url: $$url$1W
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1V = createAstro();
const $$Index$1V = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1V, $$props, $$slots);
  Astro2.self = $$Index$1V;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/solutions/protect-websites-and-applications/index.astro");

const $$file$1V = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/solutions/protect-websites-and-applications/index.astro";
const $$url$1V = "/cn/solutions/protect-websites-and-applications";

const index$1V = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1V,
   file: $$file$1V,
   url: $$url$1V
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1U = createAstro();
const $$Index$1U = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1U, $$props, $$slots);
  Astro2.self = $$Index$1U;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/solutions/maximizing-website-performance/index.astro");

const $$file$1U = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/solutions/maximizing-website-performance/index.astro";
const $$url$1U = "/cn/solutions/maximizing-website-performance";

const index$1U = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1U,
   file: $$file$1U,
   url: $$url$1U
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1T = createAstro();
const $$Index$1T = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1T, $$props, $$slots);
  Astro2.self = $$Index$1T;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/solutions/broadcast-transformation/index.astro");

const $$file$1T = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/solutions/broadcast-transformation/index.astro";
const $$url$1T = "/cn/solutions/broadcast-transformation";

const index$1T = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1T,
   file: $$file$1T,
   url: $$url$1T
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1S = createAstro();
const $$Index$1S = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1S, $$props, $$slots);
  Astro2.self = $$Index$1S;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/solutions/developer-team-velocity/index.astro");

const $$file$1S = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/solutions/developer-team-velocity/index.astro";
const $$url$1S = "/cn/solutions/developer-team-velocity";

const index$1S = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1S,
   file: $$file$1S,
   url: $$url$1S
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1R = createAstro();
const $$Index$1R = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1R, $$props, $$slots);
  Astro2.self = $$Index$1R;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/solutions/media-and-entertainment/index.astro");

const $$file$1R = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/solutions/media-and-entertainment/index.astro";
const $$url$1R = "/cn/solutions/media-and-entertainment";

const index$1R = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1R,
   file: $$file$1R,
   url: $$url$1R
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1Q = createAstro();
const $$Index$1Q = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1Q, $$props, $$slots);
  Astro2.self = $$Index$1Q;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/solutions/live-streaming/index.astro");

const $$file$1Q = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/solutions/live-streaming/index.astro";
const $$url$1Q = "/cn/solutions/live-streaming";

const index$1Q = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1Q,
   file: $$file$1Q,
   url: $$url$1Q
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1P = createAstro();
const $$Index$1P = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1P, $$props, $$slots);
  Astro2.self = $$Index$1P;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/company/leadership/index.astro");

const $$file$1P = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/company/leadership/index.astro";
const $$url$1P = "/cn/company/leadership";

const index$1P = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1P,
   file: $$file$1P,
   url: $$url$1P
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1O = createAstro();
const $$Index$1O = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1O, $$props, $$slots);
  Astro2.self = $$Index$1O;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/company/about-us/index.astro");

const $$file$1O = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/company/about-us/index.astro";
const $$url$1O = "/cn/company/about-us";

const index$1O = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1O,
   file: $$file$1O,
   url: $$url$1O
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1N = createAstro();
const $$Index$1N = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1N, $$props, $$slots);
  Astro2.self = $$Index$1N;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/media/index.astro");

const $$file$1N = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/media/index.astro";
const $$url$1N = "/cn/media";

const index$1N = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1N,
   file: $$file$1N,
   url: $$url$1N
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1M = createAstro();
const $$Index$1M = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1M, $$props, $$slots);
  Astro2.self = $$Index$1M;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/media/open-edge/index.astro");

const $$file$1M = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/media/open-edge/index.astro";
const $$url$1M = "/cn/media/open-edge";

const index$1M = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1M,
   file: $$file$1M,
   url: $$url$1M
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1L = createAstro();
const $$Index$1L = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1L, $$props, $$slots);
  Astro2.self = $$Index$1L;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/media/delivery/index.astro");

const $$file$1L = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/media/delivery/index.astro";
const $$url$1L = "/cn/media/delivery";

const index$1L = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1L,
   file: $$file$1L,
   url: $$url$1L
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1K = createAstro();
const $$Index$1K = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1K, $$props, $$slots);
  Astro2.self = $$Index$1K;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/media/uplynk/index.astro");

const $$file$1K = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/media/uplynk/index.astro";
const $$url$1K = "/cn/media/uplynk";

const index$1K = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1K,
   file: $$file$1K,
   url: $$url$1K
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1J = createAstro();
const $$Index$1J = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1J, $$props, $$slots);
  Astro2.self = $$Index$1J;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/app/index.astro");

const $$file$1J = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/app/index.astro";
const $$url$1J = "/cn/app";

const index$1J = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1J,
   file: $$file$1J,
   url: $$url$1J
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1I = createAstro();
const $$Index$1I = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1I, $$props, $$slots);
  Astro2.self = $$Index$1I;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/app/performance/index.astro");

const $$file$1I = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/app/performance/index.astro";
const $$url$1I = "/cn/app/performance";

const index$1I = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1I,
   file: $$file$1I,
   url: $$url$1I
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1H = createAstro();
const $$Index$1H = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1H, $$props, $$slots);
  Astro2.self = $$Index$1H;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/app/security/index.astro");

const $$file$1H = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/app/security/index.astro";
const $$url$1H = "/cn/app/security";

const index$1H = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1H,
   file: $$file$1H,
   url: $$url$1H
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1G = createAstro();
const $$Index$1G = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1G, $$props, $$slots);
  Astro2.self = $$Index$1G;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/app/security/advanced-bot-manager/index.astro");

const $$file$1G = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/app/security/advanced-bot-manager/index.astro";
const $$url$1G = "/cn/app/security/advanced-bot-manager";

const index$1G = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1G,
   file: $$file$1G,
   url: $$url$1G
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1F = createAstro();
const $$Index$1F = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1F, $$props, $$slots);
  Astro2.self = $$Index$1F;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/app/sites/index.astro");

const $$file$1F = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/cn/app/sites/index.astro";
const $$url$1F = "/cn/app/sites";

const index$1F = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1F,
   file: $$file$1F,
   url: $$url$1F
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1E = createAstro();
const $$Index$1E = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1E, $$props, $$slots);
  Astro2.self = $$Index$1E;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/index.astro");

const $$file$1E = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/index.astro";
const $$url$1E = "/de";

const index$1E = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1E,
   file: $$file$1E,
   url: $$url$1E
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1D = createAstro();
const $$Index$1D = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1D, $$props, $$slots);
  Astro2.self = $$Index$1D;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/resources/blog/edgio-earns-cyber-defense-global-infosec-award-at-rsa-2023/index.astro");

const $$file$1D = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/resources/blog/edgio-earns-cyber-defense-global-infosec-award-at-rsa-2023/index.astro";
const $$url$1D = "/de/resources/blog/edgio-earns-cyber-defense-global-infosec-award-at-rsa-2023";

const index$1D = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1D,
   file: $$file$1D,
   url: $$url$1D
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1C = createAstro();
const $$Index$1C = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1C, $$props, $$slots);
  Astro2.self = $$Index$1C;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/resources/blog/future-of-cybersecurity-edgio-observations-from-rsa-2023/index.astro");

const $$file$1C = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/resources/blog/future-of-cybersecurity-edgio-observations-from-rsa-2023/index.astro";
const $$url$1C = "/de/resources/blog/future-of-cybersecurity-edgio-observations-from-rsa-2023";

const index$1C = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1C,
   file: $$file$1C,
   url: $$url$1C
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1B = createAstro();
const $$Index$1B = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1B, $$props, $$slots);
  Astro2.self = $$Index$1B;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/resources/blog/edge-platforms-deliver-three-key-business-benefits/index.astro");

const $$file$1B = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/resources/blog/edge-platforms-deliver-three-key-business-benefits/index.astro";
const $$url$1B = "/de/resources/blog/edge-platforms-deliver-three-key-business-benefits";

const index$1B = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1B,
   file: $$file$1B,
   url: $$url$1B
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1A = createAstro();
const $$Index$1A = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1A, $$props, $$slots);
  Astro2.self = $$Index$1A;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/solutions/protect-websites-and-applications/index.astro");

const $$file$1A = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/solutions/protect-websites-and-applications/index.astro";
const $$url$1A = "/de/solutions/protect-websites-and-applications";

const index$1A = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1A,
   file: $$file$1A,
   url: $$url$1A
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1z = createAstro();
const $$Index$1z = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1z, $$props, $$slots);
  Astro2.self = $$Index$1z;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/solutions/maximizing-website-performance/index.astro");

const $$file$1z = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/solutions/maximizing-website-performance/index.astro";
const $$url$1z = "/de/solutions/maximizing-website-performance";

const index$1z = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1z,
   file: $$file$1z,
   url: $$url$1z
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1y = createAstro();
const $$Index$1y = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1y, $$props, $$slots);
  Astro2.self = $$Index$1y;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/solutions/broadcast-transformation/index.astro");

const $$file$1y = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/solutions/broadcast-transformation/index.astro";
const $$url$1y = "/de/solutions/broadcast-transformation";

const index$1y = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1y,
   file: $$file$1y,
   url: $$url$1y
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1x = createAstro();
const $$Index$1x = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1x, $$props, $$slots);
  Astro2.self = $$Index$1x;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/solutions/developer-team-velocity/index.astro");

const $$file$1x = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/solutions/developer-team-velocity/index.astro";
const $$url$1x = "/de/solutions/developer-team-velocity";

const index$1x = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1x,
   file: $$file$1x,
   url: $$url$1x
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1w = createAstro();
const $$Index$1w = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1w, $$props, $$slots);
  Astro2.self = $$Index$1w;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/solutions/media-and-entertainment/index.astro");

const $$file$1w = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/solutions/media-and-entertainment/index.astro";
const $$url$1w = "/de/solutions/media-and-entertainment";

const index$1w = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1w,
   file: $$file$1w,
   url: $$url$1w
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1v = createAstro();
const $$Index$1v = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1v, $$props, $$slots);
  Astro2.self = $$Index$1v;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/solutions/live-streaming/index.astro");

const $$file$1v = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/solutions/live-streaming/index.astro";
const $$url$1v = "/de/solutions/live-streaming";

const index$1v = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1v,
   file: $$file$1v,
   url: $$url$1v
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1u = createAstro();
const $$Index$1u = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1u, $$props, $$slots);
  Astro2.self = $$Index$1u;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/company/leadership/index.astro");

const $$file$1u = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/company/leadership/index.astro";
const $$url$1u = "/de/company/leadership";

const index$1u = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1u,
   file: $$file$1u,
   url: $$url$1u
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1t = createAstro();
const $$Index$1t = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1t, $$props, $$slots);
  Astro2.self = $$Index$1t;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/company/about-us/index.astro");

const $$file$1t = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/company/about-us/index.astro";
const $$url$1t = "/de/company/about-us";

const index$1t = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1t,
   file: $$file$1t,
   url: $$url$1t
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1s = createAstro();
const $$Index$1s = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1s, $$props, $$slots);
  Astro2.self = $$Index$1s;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/media/index.astro");

const $$file$1s = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/media/index.astro";
const $$url$1s = "/de/media";

const index$1s = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1s,
   file: $$file$1s,
   url: $$url$1s
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1r = createAstro();
const $$Index$1r = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1r, $$props, $$slots);
  Astro2.self = $$Index$1r;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/media/open-edge/index.astro");

const $$file$1r = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/media/open-edge/index.astro";
const $$url$1r = "/de/media/open-edge";

const index$1r = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1r,
   file: $$file$1r,
   url: $$url$1r
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1q = createAstro();
const $$Index$1q = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1q, $$props, $$slots);
  Astro2.self = $$Index$1q;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/media/delivery/index.astro");

const $$file$1q = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/media/delivery/index.astro";
const $$url$1q = "/de/media/delivery";

const index$1q = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1q,
   file: $$file$1q,
   url: $$url$1q
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1p = createAstro();
const $$Index$1p = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1p, $$props, $$slots);
  Astro2.self = $$Index$1p;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/media/uplynk/index.astro");

const $$file$1p = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/media/uplynk/index.astro";
const $$url$1p = "/de/media/uplynk";

const index$1p = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1p,
   file: $$file$1p,
   url: $$url$1p
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1o = createAstro();
const $$Index$1o = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1o, $$props, $$slots);
  Astro2.self = $$Index$1o;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/app/index.astro");

const $$file$1o = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/app/index.astro";
const $$url$1o = "/de/app";

const index$1o = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1o,
   file: $$file$1o,
   url: $$url$1o
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1n = createAstro();
const $$Index$1n = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1n, $$props, $$slots);
  Astro2.self = $$Index$1n;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/app/performance/index.astro");

const $$file$1n = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/app/performance/index.astro";
const $$url$1n = "/de/app/performance";

const index$1n = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1n,
   file: $$file$1n,
   url: $$url$1n
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1m = createAstro();
const $$Index$1m = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1m, $$props, $$slots);
  Astro2.self = $$Index$1m;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/app/security/index.astro");

const $$file$1m = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/app/security/index.astro";
const $$url$1m = "/de/app/security";

const index$1m = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1m,
   file: $$file$1m,
   url: $$url$1m
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1l = createAstro();
const $$Index$1l = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1l, $$props, $$slots);
  Astro2.self = $$Index$1l;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/app/security/advanced-bot-manager/index.astro");

const $$file$1l = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/app/security/advanced-bot-manager/index.astro";
const $$url$1l = "/de/app/security/advanced-bot-manager";

const index$1l = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1l,
   file: $$file$1l,
   url: $$url$1l
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1k = createAstro();
const $$Index$1k = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1k, $$props, $$slots);
  Astro2.self = $$Index$1k;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/app/sites/index.astro");

const $$file$1k = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/de/app/sites/index.astro";
const $$url$1k = "/de/app/sites";

const index$1k = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1k,
   file: $$file$1k,
   url: $$url$1k
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1j = createAstro();
const $$Index$1j = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1j, $$props, $$slots);
  Astro2.self = $$Index$1j;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/index.astro");

const $$file$1j = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/index.astro";
const $$url$1j = "/fr";

const index$1j = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1j,
   file: $$file$1j,
   url: $$url$1j
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1i = createAstro();
const $$Index$1i = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1i, $$props, $$slots);
  Astro2.self = $$Index$1i;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/resources/blog/edgio-earns-cyber-defense-global-infosec-award-at-rsa-2023/index.astro");

const $$file$1i = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/resources/blog/edgio-earns-cyber-defense-global-infosec-award-at-rsa-2023/index.astro";
const $$url$1i = "/fr/resources/blog/edgio-earns-cyber-defense-global-infosec-award-at-rsa-2023";

const index$1i = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1i,
   file: $$file$1i,
   url: $$url$1i
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1h = createAstro();
const $$Index$1h = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1h, $$props, $$slots);
  Astro2.self = $$Index$1h;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/resources/blog/future-of-cybersecurity-edgio-observations-from-rsa-2023/index.astro");

const $$file$1h = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/resources/blog/future-of-cybersecurity-edgio-observations-from-rsa-2023/index.astro";
const $$url$1h = "/fr/resources/blog/future-of-cybersecurity-edgio-observations-from-rsa-2023";

const index$1h = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1h,
   file: $$file$1h,
   url: $$url$1h
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1g = createAstro();
const $$Index$1g = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1g, $$props, $$slots);
  Astro2.self = $$Index$1g;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/resources/blog/edge-platforms-deliver-three-key-business-benefits/index.astro");

const $$file$1g = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/resources/blog/edge-platforms-deliver-three-key-business-benefits/index.astro";
const $$url$1g = "/fr/resources/blog/edge-platforms-deliver-three-key-business-benefits";

const index$1g = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1g,
   file: $$file$1g,
   url: $$url$1g
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1f = createAstro();
const $$Index$1f = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1f, $$props, $$slots);
  Astro2.self = $$Index$1f;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/solutions/protect-websites-and-applications/index.astro");

const $$file$1f = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/solutions/protect-websites-and-applications/index.astro";
const $$url$1f = "/fr/solutions/protect-websites-and-applications";

const index$1f = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1f,
   file: $$file$1f,
   url: $$url$1f
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1e = createAstro();
const $$Index$1e = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1e, $$props, $$slots);
  Astro2.self = $$Index$1e;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/solutions/maximizing-website-performance/index.astro");

const $$file$1e = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/solutions/maximizing-website-performance/index.astro";
const $$url$1e = "/fr/solutions/maximizing-website-performance";

const index$1e = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1e,
   file: $$file$1e,
   url: $$url$1e
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1d = createAstro();
const $$Index$1d = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1d, $$props, $$slots);
  Astro2.self = $$Index$1d;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/solutions/broadcast-transformation/index.astro");

const $$file$1d = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/solutions/broadcast-transformation/index.astro";
const $$url$1d = "/fr/solutions/broadcast-transformation";

const index$1d = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1d,
   file: $$file$1d,
   url: $$url$1d
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1c = createAstro();
const $$Index$1c = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1c, $$props, $$slots);
  Astro2.self = $$Index$1c;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/solutions/developer-team-velocity/index.astro");

const $$file$1c = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/solutions/developer-team-velocity/index.astro";
const $$url$1c = "/fr/solutions/developer-team-velocity";

const index$1c = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1c,
   file: $$file$1c,
   url: $$url$1c
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1b = createAstro();
const $$Index$1b = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1b, $$props, $$slots);
  Astro2.self = $$Index$1b;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/solutions/media-and-entertainment/index.astro");

const $$file$1b = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/solutions/media-and-entertainment/index.astro";
const $$url$1b = "/fr/solutions/media-and-entertainment";

const index$1b = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1b,
   file: $$file$1b,
   url: $$url$1b
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1a = createAstro();
const $$Index$1a = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1a, $$props, $$slots);
  Astro2.self = $$Index$1a;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/solutions/live-streaming/index.astro");

const $$file$1a = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/solutions/live-streaming/index.astro";
const $$url$1a = "/fr/solutions/live-streaming";

const index$1a = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1a,
   file: $$file$1a,
   url: $$url$1a
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$19 = createAstro();
const $$Index$19 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$19, $$props, $$slots);
  Astro2.self = $$Index$19;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/company/leadership/index.astro");

const $$file$19 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/company/leadership/index.astro";
const $$url$19 = "/fr/company/leadership";

const index$19 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$19,
   file: $$file$19,
   url: $$url$19
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$18 = createAstro();
const $$Index$18 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$18, $$props, $$slots);
  Astro2.self = $$Index$18;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/company/about-us/index.astro");

const $$file$18 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/company/about-us/index.astro";
const $$url$18 = "/fr/company/about-us";

const index$18 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$18,
   file: $$file$18,
   url: $$url$18
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$17 = createAstro();
const $$Index$17 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$17, $$props, $$slots);
  Astro2.self = $$Index$17;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/media/index.astro");

const $$file$17 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/media/index.astro";
const $$url$17 = "/fr/media";

const index$17 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$17,
   file: $$file$17,
   url: $$url$17
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$16 = createAstro();
const $$Index$16 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$16, $$props, $$slots);
  Astro2.self = $$Index$16;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/media/open-edge/index.astro");

const $$file$16 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/media/open-edge/index.astro";
const $$url$16 = "/fr/media/open-edge";

const index$16 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$16,
   file: $$file$16,
   url: $$url$16
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$15 = createAstro();
const $$Index$15 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$15, $$props, $$slots);
  Astro2.self = $$Index$15;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/media/delivery/index.astro");

const $$file$15 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/media/delivery/index.astro";
const $$url$15 = "/fr/media/delivery";

const index$15 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$15,
   file: $$file$15,
   url: $$url$15
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$14 = createAstro();
const $$Index$14 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$14, $$props, $$slots);
  Astro2.self = $$Index$14;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/media/uplynk/index.astro");

const $$file$14 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/media/uplynk/index.astro";
const $$url$14 = "/fr/media/uplynk";

const index$14 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$14,
   file: $$file$14,
   url: $$url$14
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$13 = createAstro();
const $$Index$13 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$13, $$props, $$slots);
  Astro2.self = $$Index$13;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/app/index.astro");

const $$file$13 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/app/index.astro";
const $$url$13 = "/fr/app";

const index$13 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$13,
   file: $$file$13,
   url: $$url$13
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$12 = createAstro();
const $$Index$12 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$12, $$props, $$slots);
  Astro2.self = $$Index$12;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/app/performance/index.astro");

const $$file$12 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/app/performance/index.astro";
const $$url$12 = "/fr/app/performance";

const index$12 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$12,
   file: $$file$12,
   url: $$url$12
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$11 = createAstro();
const $$Index$11 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$11, $$props, $$slots);
  Astro2.self = $$Index$11;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/app/security/index.astro");

const $$file$11 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/app/security/index.astro";
const $$url$11 = "/fr/app/security";

const index$11 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$11,
   file: $$file$11,
   url: $$url$11
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$10 = createAstro();
const $$Index$10 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$10, $$props, $$slots);
  Astro2.self = $$Index$10;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/app/security/advanced-bot-manager/index.astro");

const $$file$10 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/app/security/advanced-bot-manager/index.astro";
const $$url$10 = "/fr/app/security/advanced-bot-manager";

const index$10 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$10,
   file: $$file$10,
   url: $$url$10
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$$ = createAstro();
const $$Index$$ = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$$, $$props, $$slots);
  Astro2.self = $$Index$$;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/app/sites/index.astro");

const $$file$$ = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/fr/app/sites/index.astro";
const $$url$$ = "/fr/app/sites";

const index$$ = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$$,
   file: $$file$$,
   url: $$url$$
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$_ = createAstro();
const $$Index$_ = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$_, $$props, $$slots);
  Astro2.self = $$Index$_;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/index.astro");

const $$file$_ = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/index.astro";
const $$url$_ = "/jp";

const index$_ = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$_,
   file: $$file$_,
   url: $$url$_
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$Z = createAstro();
const $$Index$Z = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$Z, $$props, $$slots);
  Astro2.self = $$Index$Z;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/resources/blog/edgio-earns-cyber-defense-global-infosec-award-at-rsa-2023/index.astro");

const $$file$Z = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/resources/blog/edgio-earns-cyber-defense-global-infosec-award-at-rsa-2023/index.astro";
const $$url$Z = "/jp/resources/blog/edgio-earns-cyber-defense-global-infosec-award-at-rsa-2023";

const index$Z = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$Z,
   file: $$file$Z,
   url: $$url$Z
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$Y = createAstro();
const $$Index$Y = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$Y, $$props, $$slots);
  Astro2.self = $$Index$Y;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/resources/blog/future-of-cybersecurity-edgio-observations-from-rsa-2023/index.astro");

const $$file$Y = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/resources/blog/future-of-cybersecurity-edgio-observations-from-rsa-2023/index.astro";
const $$url$Y = "/jp/resources/blog/future-of-cybersecurity-edgio-observations-from-rsa-2023";

const index$Y = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$Y,
   file: $$file$Y,
   url: $$url$Y
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$X = createAstro();
const $$Index$X = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$X, $$props, $$slots);
  Astro2.self = $$Index$X;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/resources/blog/edge-platforms-deliver-three-key-business-benefits/index.astro");

const $$file$X = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/resources/blog/edge-platforms-deliver-three-key-business-benefits/index.astro";
const $$url$X = "/jp/resources/blog/edge-platforms-deliver-three-key-business-benefits";

const index$X = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$X,
   file: $$file$X,
   url: $$url$X
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$W = createAstro();
const $$Index$W = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$W, $$props, $$slots);
  Astro2.self = $$Index$W;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/solutions/protect-websites-and-applications/index.astro");

const $$file$W = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/solutions/protect-websites-and-applications/index.astro";
const $$url$W = "/jp/solutions/protect-websites-and-applications";

const index$W = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$W,
   file: $$file$W,
   url: $$url$W
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$V = createAstro();
const $$Index$V = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$V, $$props, $$slots);
  Astro2.self = $$Index$V;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/solutions/maximizing-website-performance/index.astro");

const $$file$V = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/solutions/maximizing-website-performance/index.astro";
const $$url$V = "/jp/solutions/maximizing-website-performance";

const index$V = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$V,
   file: $$file$V,
   url: $$url$V
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$U = createAstro();
const $$Index$U = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$U, $$props, $$slots);
  Astro2.self = $$Index$U;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/solutions/broadcast-transformation/index.astro");

const $$file$U = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/solutions/broadcast-transformation/index.astro";
const $$url$U = "/jp/solutions/broadcast-transformation";

const index$U = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$U,
   file: $$file$U,
   url: $$url$U
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$T = createAstro();
const $$Index$T = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$T, $$props, $$slots);
  Astro2.self = $$Index$T;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/solutions/developer-team-velocity/index.astro");

const $$file$T = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/solutions/developer-team-velocity/index.astro";
const $$url$T = "/jp/solutions/developer-team-velocity";

const index$T = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$T,
   file: $$file$T,
   url: $$url$T
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$S = createAstro();
const $$Index$S = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$S, $$props, $$slots);
  Astro2.self = $$Index$S;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/solutions/media-and-entertainment/index.astro");

const $$file$S = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/solutions/media-and-entertainment/index.astro";
const $$url$S = "/jp/solutions/media-and-entertainment";

const index$S = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$S,
   file: $$file$S,
   url: $$url$S
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$R = createAstro();
const $$Index$R = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$R, $$props, $$slots);
  Astro2.self = $$Index$R;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/solutions/live-streaming/index.astro");

const $$file$R = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/solutions/live-streaming/index.astro";
const $$url$R = "/jp/solutions/live-streaming";

const index$R = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$R,
   file: $$file$R,
   url: $$url$R
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$Q = createAstro();
const $$Index$Q = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$Q, $$props, $$slots);
  Astro2.self = $$Index$Q;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/company/leadership/index.astro");

const $$file$Q = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/company/leadership/index.astro";
const $$url$Q = "/jp/company/leadership";

const index$Q = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$Q,
   file: $$file$Q,
   url: $$url$Q
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$P = createAstro();
const $$Index$P = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$P, $$props, $$slots);
  Astro2.self = $$Index$P;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/company/about-us/index.astro");

const $$file$P = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/company/about-us/index.astro";
const $$url$P = "/jp/company/about-us";

const index$P = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$P,
   file: $$file$P,
   url: $$url$P
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$O = createAstro();
const $$Index$O = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$O, $$props, $$slots);
  Astro2.self = $$Index$O;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/media/index.astro");

const $$file$O = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/media/index.astro";
const $$url$O = "/jp/media";

const index$O = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$O,
   file: $$file$O,
   url: $$url$O
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$N = createAstro();
const $$Index$N = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$N, $$props, $$slots);
  Astro2.self = $$Index$N;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/media/open-edge/index.astro");

const $$file$N = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/media/open-edge/index.astro";
const $$url$N = "/jp/media/open-edge";

const index$N = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$N,
   file: $$file$N,
   url: $$url$N
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$M = createAstro();
const $$Index$M = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$M, $$props, $$slots);
  Astro2.self = $$Index$M;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/media/delivery/index.astro");

const $$file$M = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/media/delivery/index.astro";
const $$url$M = "/jp/media/delivery";

const index$M = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$M,
   file: $$file$M,
   url: $$url$M
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$L = createAstro();
const $$Index$L = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$L, $$props, $$slots);
  Astro2.self = $$Index$L;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/media/uplynk/index.astro");

const $$file$L = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/media/uplynk/index.astro";
const $$url$L = "/jp/media/uplynk";

const index$L = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$L,
   file: $$file$L,
   url: $$url$L
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$K = createAstro();
const $$Index$K = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$K, $$props, $$slots);
  Astro2.self = $$Index$K;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/app/index.astro");

const $$file$K = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/app/index.astro";
const $$url$K = "/jp/app";

const index$K = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$K,
   file: $$file$K,
   url: $$url$K
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$J = createAstro();
const $$Index$J = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$J, $$props, $$slots);
  Astro2.self = $$Index$J;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/app/performance/index.astro");

const $$file$J = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/app/performance/index.astro";
const $$url$J = "/jp/app/performance";

const index$J = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$J,
   file: $$file$J,
   url: $$url$J
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$I = createAstro();
const $$Index$I = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$I, $$props, $$slots);
  Astro2.self = $$Index$I;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/app/security/index.astro");

const $$file$I = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/app/security/index.astro";
const $$url$I = "/jp/app/security";

const index$I = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$I,
   file: $$file$I,
   url: $$url$I
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$H = createAstro();
const $$Index$H = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$H, $$props, $$slots);
  Astro2.self = $$Index$H;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/app/security/advanced-bot-manager/index.astro");

const $$file$H = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/app/security/advanced-bot-manager/index.astro";
const $$url$H = "/jp/app/security/advanced-bot-manager";

const index$H = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$H,
   file: $$file$H,
   url: $$url$H
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$G = createAstro();
const $$Index$G = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$G, $$props, $$slots);
  Astro2.self = $$Index$G;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/app/sites/index.astro");

const $$file$G = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/jp/app/sites/index.astro";
const $$url$G = "/jp/app/sites";

const index$G = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$G,
   file: $$file$G,
   url: $$url$G
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$F = createAstro();
const $$Index$F = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$F, $$props, $$slots);
  Astro2.self = $$Index$F;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/index.astro");

const $$file$F = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/index.astro";
const $$url$F = "/kr";

const index$F = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$F,
   file: $$file$F,
   url: $$url$F
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$E = createAstro();
const $$Index$E = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$E, $$props, $$slots);
  Astro2.self = $$Index$E;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/resources/blog/edgio-earns-cyber-defense-global-infosec-award-at-rsa-2023/index.astro");

const $$file$E = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/resources/blog/edgio-earns-cyber-defense-global-infosec-award-at-rsa-2023/index.astro";
const $$url$E = "/kr/resources/blog/edgio-earns-cyber-defense-global-infosec-award-at-rsa-2023";

const index$E = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$E,
   file: $$file$E,
   url: $$url$E
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$D = createAstro();
const $$Index$D = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$D, $$props, $$slots);
  Astro2.self = $$Index$D;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/resources/blog/future-of-cybersecurity-edgio-observations-from-rsa-2023/index.astro");

const $$file$D = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/resources/blog/future-of-cybersecurity-edgio-observations-from-rsa-2023/index.astro";
const $$url$D = "/kr/resources/blog/future-of-cybersecurity-edgio-observations-from-rsa-2023";

const index$D = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$D,
   file: $$file$D,
   url: $$url$D
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$C = createAstro();
const $$Index$C = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$C, $$props, $$slots);
  Astro2.self = $$Index$C;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/resources/blog/edge-platforms-deliver-three-key-business-benefits/index.astro");

const $$file$C = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/resources/blog/edge-platforms-deliver-three-key-business-benefits/index.astro";
const $$url$C = "/kr/resources/blog/edge-platforms-deliver-three-key-business-benefits";

const index$C = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$C,
   file: $$file$C,
   url: $$url$C
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$B = createAstro();
const $$Index$B = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$B, $$props, $$slots);
  Astro2.self = $$Index$B;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/solutions/protect-websites-and-applications/index.astro");

const $$file$B = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/solutions/protect-websites-and-applications/index.astro";
const $$url$B = "/kr/solutions/protect-websites-and-applications";

const index$B = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$B,
   file: $$file$B,
   url: $$url$B
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$A = createAstro();
const $$Index$A = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$A, $$props, $$slots);
  Astro2.self = $$Index$A;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/solutions/maximizing-website-performance/index.astro");

const $$file$A = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/solutions/maximizing-website-performance/index.astro";
const $$url$A = "/kr/solutions/maximizing-website-performance";

const index$A = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$A,
   file: $$file$A,
   url: $$url$A
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$z = createAstro();
const $$Index$z = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$z, $$props, $$slots);
  Astro2.self = $$Index$z;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/solutions/broadcast-transformation/index.astro");

const $$file$z = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/solutions/broadcast-transformation/index.astro";
const $$url$z = "/kr/solutions/broadcast-transformation";

const index$z = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$z,
   file: $$file$z,
   url: $$url$z
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$y = createAstro();
const $$Index$y = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$y, $$props, $$slots);
  Astro2.self = $$Index$y;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/solutions/developer-team-velocity/index.astro");

const $$file$y = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/solutions/developer-team-velocity/index.astro";
const $$url$y = "/kr/solutions/developer-team-velocity";

const index$y = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$y,
   file: $$file$y,
   url: $$url$y
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$x = createAstro();
const $$Index$x = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$x, $$props, $$slots);
  Astro2.self = $$Index$x;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/solutions/media-and-entertainment/index.astro");

const $$file$x = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/solutions/media-and-entertainment/index.astro";
const $$url$x = "/kr/solutions/media-and-entertainment";

const index$x = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$x,
   file: $$file$x,
   url: $$url$x
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$w = createAstro();
const $$Index$w = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$w, $$props, $$slots);
  Astro2.self = $$Index$w;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/solutions/live-streaming/index.astro");

const $$file$w = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/solutions/live-streaming/index.astro";
const $$url$w = "/kr/solutions/live-streaming";

const index$w = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$w,
   file: $$file$w,
   url: $$url$w
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$v = createAstro();
const $$Index$v = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$v, $$props, $$slots);
  Astro2.self = $$Index$v;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/company/leadership/index.astro");

const $$file$v = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/company/leadership/index.astro";
const $$url$v = "/kr/company/leadership";

const index$v = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$v,
   file: $$file$v,
   url: $$url$v
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$u = createAstro();
const $$Index$u = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$u, $$props, $$slots);
  Astro2.self = $$Index$u;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/company/about-us/index.astro");

const $$file$u = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/company/about-us/index.astro";
const $$url$u = "/kr/company/about-us";

const index$u = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$u,
   file: $$file$u,
   url: $$url$u
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$t = createAstro();
const $$Index$t = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$t, $$props, $$slots);
  Astro2.self = $$Index$t;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/media/index.astro");

const $$file$t = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/media/index.astro";
const $$url$t = "/kr/media";

const index$t = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$t,
   file: $$file$t,
   url: $$url$t
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$s = createAstro();
const $$Index$s = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$s, $$props, $$slots);
  Astro2.self = $$Index$s;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/media/open-edge/index.astro");

const $$file$s = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/media/open-edge/index.astro";
const $$url$s = "/kr/media/open-edge";

const index$s = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$s,
   file: $$file$s,
   url: $$url$s
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$r = createAstro();
const $$Index$r = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$r, $$props, $$slots);
  Astro2.self = $$Index$r;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/media/delivery/index.astro");

const $$file$r = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/media/delivery/index.astro";
const $$url$r = "/kr/media/delivery";

const index$r = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$r,
   file: $$file$r,
   url: $$url$r
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$q = createAstro();
const $$Index$q = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$q, $$props, $$slots);
  Astro2.self = $$Index$q;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/media/uplynk/index.astro");

const $$file$q = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/media/uplynk/index.astro";
const $$url$q = "/kr/media/uplynk";

const index$q = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$q,
   file: $$file$q,
   url: $$url$q
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$p = createAstro();
const $$Index$p = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$p, $$props, $$slots);
  Astro2.self = $$Index$p;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/app/index.astro");

const $$file$p = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/app/index.astro";
const $$url$p = "/kr/app";

const index$p = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$p,
   file: $$file$p,
   url: $$url$p
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$o = createAstro();
const $$Index$o = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$o, $$props, $$slots);
  Astro2.self = $$Index$o;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/app/performance/index.astro");

const $$file$o = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/app/performance/index.astro";
const $$url$o = "/kr/app/performance";

const index$o = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$o,
   file: $$file$o,
   url: $$url$o
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$n = createAstro();
const $$Index$n = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$n, $$props, $$slots);
  Astro2.self = $$Index$n;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/app/security/index.astro");

const $$file$n = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/app/security/index.astro";
const $$url$n = "/kr/app/security";

const index$n = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$n,
   file: $$file$n,
   url: $$url$n
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$m = createAstro();
const $$Index$m = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$m, $$props, $$slots);
  Astro2.self = $$Index$m;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/app/security/advanced-bot-manager/index.astro");

const $$file$m = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/app/security/advanced-bot-manager/index.astro";
const $$url$m = "/kr/app/security/advanced-bot-manager";

const index$m = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$m,
   file: $$file$m,
   url: $$url$m
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$l = createAstro();
const $$Index$l = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$l, $$props, $$slots);
  Astro2.self = $$Index$l;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/app/sites/index.astro");

const $$file$l = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/kr/app/sites/index.astro";
const $$url$l = "/kr/app/sites";

const index$l = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$l,
   file: $$file$l,
   url: $$url$l
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$k = createAstro();
const $$Index$k = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$k, $$props, $$slots);
  Astro2.self = $$Index$k;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/index.astro");

const $$file$k = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/index.astro";
const $$url$k = "/tw";

const index$k = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$k,
   file: $$file$k,
   url: $$url$k
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$j = createAstro();
const $$Index$j = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$j, $$props, $$slots);
  Astro2.self = $$Index$j;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/resources/blog/edgio-earns-cyber-defense-global-infosec-award-at-rsa-2023/index.astro");

const $$file$j = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/resources/blog/edgio-earns-cyber-defense-global-infosec-award-at-rsa-2023/index.astro";
const $$url$j = "/tw/resources/blog/edgio-earns-cyber-defense-global-infosec-award-at-rsa-2023";

const index$j = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$j,
   file: $$file$j,
   url: $$url$j
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$i = createAstro();
const $$Index$i = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$i, $$props, $$slots);
  Astro2.self = $$Index$i;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/resources/blog/future-of-cybersecurity-edgio-observations-from-rsa-2023/index.astro");

const $$file$i = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/resources/blog/future-of-cybersecurity-edgio-observations-from-rsa-2023/index.astro";
const $$url$i = "/tw/resources/blog/future-of-cybersecurity-edgio-observations-from-rsa-2023";

const index$i = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$i,
   file: $$file$i,
   url: $$url$i
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$h = createAstro();
const $$Index$h = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$h, $$props, $$slots);
  Astro2.self = $$Index$h;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/resources/blog/edge-platforms-deliver-three-key-business-benefits/index.astro");

const $$file$h = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/resources/blog/edge-platforms-deliver-three-key-business-benefits/index.astro";
const $$url$h = "/tw/resources/blog/edge-platforms-deliver-three-key-business-benefits";

const index$h = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$h,
   file: $$file$h,
   url: $$url$h
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$g = createAstro();
const $$Index$g = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$g, $$props, $$slots);
  Astro2.self = $$Index$g;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/solutions/protect-websites-and-applications/index.astro");

const $$file$g = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/solutions/protect-websites-and-applications/index.astro";
const $$url$g = "/tw/solutions/protect-websites-and-applications";

const index$g = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$g,
   file: $$file$g,
   url: $$url$g
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$f = createAstro();
const $$Index$f = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$f, $$props, $$slots);
  Astro2.self = $$Index$f;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/solutions/maximizing-website-performance/index.astro");

const $$file$f = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/solutions/maximizing-website-performance/index.astro";
const $$url$f = "/tw/solutions/maximizing-website-performance";

const index$f = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$f,
   file: $$file$f,
   url: $$url$f
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$e = createAstro();
const $$Index$e = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$e, $$props, $$slots);
  Astro2.self = $$Index$e;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/solutions/broadcast-transformation/index.astro");

const $$file$e = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/solutions/broadcast-transformation/index.astro";
const $$url$e = "/tw/solutions/broadcast-transformation";

const index$e = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$e,
   file: $$file$e,
   url: $$url$e
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$d = createAstro();
const $$Index$d = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$d, $$props, $$slots);
  Astro2.self = $$Index$d;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/solutions/developer-team-velocity/index.astro");

const $$file$d = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/solutions/developer-team-velocity/index.astro";
const $$url$d = "/tw/solutions/developer-team-velocity";

const index$d = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$d,
   file: $$file$d,
   url: $$url$d
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$c = createAstro();
const $$Index$c = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$c, $$props, $$slots);
  Astro2.self = $$Index$c;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/solutions/media-and-entertainment/index.astro");

const $$file$c = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/solutions/media-and-entertainment/index.astro";
const $$url$c = "/tw/solutions/media-and-entertainment";

const index$c = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$c,
   file: $$file$c,
   url: $$url$c
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$b = createAstro();
const $$Index$b = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$b, $$props, $$slots);
  Astro2.self = $$Index$b;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/solutions/live-streaming/index.astro");

const $$file$b = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/solutions/live-streaming/index.astro";
const $$url$b = "/tw/solutions/live-streaming";

const index$b = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$b,
   file: $$file$b,
   url: $$url$b
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$a = createAstro();
const $$Index$a = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$a, $$props, $$slots);
  Astro2.self = $$Index$a;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/company/leadership/index.astro");

const $$file$a = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/company/leadership/index.astro";
const $$url$a = "/tw/company/leadership";

const index$a = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$a,
   file: $$file$a,
   url: $$url$a
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$9 = createAstro();
const $$Index$9 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$9, $$props, $$slots);
  Astro2.self = $$Index$9;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/company/about-us/index.astro");

const $$file$9 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/company/about-us/index.astro";
const $$url$9 = "/tw/company/about-us";

const index$9 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$9,
   file: $$file$9,
   url: $$url$9
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$8 = createAstro();
const $$Index$8 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$8, $$props, $$slots);
  Astro2.self = $$Index$8;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/media/index.astro");

const $$file$8 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/media/index.astro";
const $$url$8 = "/tw/media";

const index$8 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$8,
   file: $$file$8,
   url: $$url$8
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$7 = createAstro();
const $$Index$7 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$7, $$props, $$slots);
  Astro2.self = $$Index$7;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/media/open-edge/index.astro");

const $$file$7 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/media/open-edge/index.astro";
const $$url$7 = "/tw/media/open-edge";

const index$7 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$7,
   file: $$file$7,
   url: $$url$7
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$6 = createAstro();
const $$Index$6 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$6, $$props, $$slots);
  Astro2.self = $$Index$6;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/media/delivery/index.astro");

const $$file$6 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/media/delivery/index.astro";
const $$url$6 = "/tw/media/delivery";

const index$6 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$6,
   file: $$file$6,
   url: $$url$6
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$5 = createAstro();
const $$Index$5 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$5, $$props, $$slots);
  Astro2.self = $$Index$5;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/media/uplynk/index.astro");

const $$file$5 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/media/uplynk/index.astro";
const $$url$5 = "/tw/media/uplynk";

const index$5 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$5,
   file: $$file$5,
   url: $$url$5
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$4 = createAstro();
const $$Index$4 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$4, $$props, $$slots);
  Astro2.self = $$Index$4;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/app/index.astro");

const $$file$4 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/app/index.astro";
const $$url$4 = "/tw/app";

const index$4 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$4,
   file: $$file$4,
   url: $$url$4
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$3 = createAstro();
const $$Index$3 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3, $$props, $$slots);
  Astro2.self = $$Index$3;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/app/performance/index.astro");

const $$file$3 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/app/performance/index.astro";
const $$url$3 = "/tw/app/performance";

const index$3 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$3,
   file: $$file$3,
   url: $$url$3
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$2 = createAstro();
const $$Index$2 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2, $$props, $$slots);
  Astro2.self = $$Index$2;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/app/security/index.astro");

const $$file$2 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/app/security/index.astro";
const $$url$2 = "/tw/app/security";

const index$2 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$2,
   file: $$file$2,
   url: $$url$2
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro$1 = createAstro();
const $$Index$1 = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1, $$props, $$slots);
  Astro2.self = $$Index$1;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/app/security/advanced-bot-manager/index.astro");

const $$file$1 = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/app/security/advanced-bot-manager/index.astro";
const $$url$1 = "/tw/app/security/advanced-bot-manager";

const index$1 = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index$1,
   file: $$file$1,
   url: $$url$1
}, Symbol.toStringTag, { value: 'Module' }));

const $$Astro = createAstro();
const $$Index = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro, $$props, $$slots);
  Astro2.self = $$Index;
  const { pathname } = Astro2.url;
  const NavElement = await fetch("https://edgio-stagining.instawp.xyz/wp-content/uploads/2023/05/nav.html");
  const Nav = await NavElement.text();
  const endpoint = "https://edg.io";
  const response = await fetch(endpoint + "/" + pathname);
  await response.text();
  const _static = await fetch("https://edgiodev.kinsta.cloud/wp-content/uploads/_static/" + pathname);
  const _response = await _static.text();
  cheerio.load(_response);
  const $ = cheerio.load(_response);
  const _ = cheerio.load(Nav);
  const navElement = $(".DpHeader_dpHeader__nav__KIxLx");
  const replacementElement = _.html();
  navElement.replaceWith(replacementElement);
  $(".informer-sections > :not(section)").each((index, element) => {
    const $element = $(element);
    const section = $("<section>");
    const attributes = $element.get(0).attribs;
    Object.keys(attributes).forEach((attr) => {
      section.attr(attr, attributes[attr]);
    });
    section.html($element.html());
    $element.replaceWith(section);
  });
  $("#onetrust-style").remove();
  $(".informer-sections").each((index, element) => {
    $(element).replaceWith($("<main>" + $(element).html() + "</main>")).attr("class", "informer-sections");
  });
  $("link").remove();
  $("script").remove();
  $("div").addClass("lc-block");
  $("div").each((index, element) => {
    if ($(element).text().trim() !== "") {
      $(element).attr("editable", "inline");
    }
  });
  $("ul, ol, blockquote, code, kbd").addClass("lc-block");
  $("p, span, li, td, th, dt, dd").attr("editable", "inline");
  $("h1, h2, h3, h4, h5, h6, a, button").attr("editable", "rich");
  $(".dp-header").attr("x-data", "{ search: false }");
  $(".DpHeader_dp-util-item__1Q-MC.DpHeader_dp-utils__item__25bYn.has-dropdown").attr("x-on:click", "isOpen = !isOpen");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-on:mouseover", "isActive = true");
  $(".DpLangSwitcher_dp-lang-switcher__10jx1").attr("x-data", "{ isActive: false }");
  $(".DpHeader_dp-mob-nav__item__2brw8.DpHeader_dp-nav-accordion__3p1Ip").attr("x-on:click", "isExpanded = !isExpanded");
  $(".DpHeader_dp-utils__item__25bYn.DpHeader_dp-navbar-trigger__3zcfD").attr("x-on:click", "isNavbarOpened = !isNavbarOpened");
  $(".DpHeader_dp-navbar__item__FAFCr").each((index, element) => {
    const $element = $(element);
    $element.attr("x-data", "{ hover: false }");
    $element.attr("x-on:mouseover", "hover = true");
    $element.attr("x-on:mouseleave", "hover = false");
    $element.attr("x-bind:class", "{ 'open-menu': hover }");
  });
  $("header").each((i, elem) => {
    const attrs = elem.attribs;
    const contents = $(elem).html();
    const newDiv = $("<div>").attr(attrs).html(contents).addClass("lc-block");
    $(elem).replaceWith(newDiv);
  });
  $("main").attr("x-data", "{ ready: true }");
  $("main").attr("class", "informer-sections");
  const head = $("head").html();
  $(".DpHeader_dp-header-wrap__mlNXY").html();
  const footer = $(".wrapper__footer").html();
  $(".TopPromo_root__2ciki").html();
  $(".DpInformerBlock_dp-invite-informer-section__2eN19").html();
  $("footer").remove();
  $("head").remove();
  const content = $(".informer-sections").html();
  $(".informer-sections").remove();
  const blocks = $("body").html();
  return renderTemplate`${renderComponent($$result, "Frame", $$Frame, {}, { "default": ($$result2) => renderTemplate`${maybeRenderHead($$result2)}<header>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(blocks)}` })}</header><main id="__artisan" class="informer-sections">${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(content)}` })}<footer>${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(footer)}` })}</footer></main>`, "headerScripts": ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "headerScripts" }, { "default": ($$result3) => renderTemplate`${unescapeHTML(head)}` })}` })}`;
}, "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/app/sites/index.astro");

const $$file = "/Users/notable/CORE/activity/projects/2-ACTIVE/edgio-projects/edgio/src/pages/tw/app/sites/index.astro";
const $$url = "/tw/app/sites";

const index = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
   __proto__: null,
   default: $$Index,
   file: $$file,
   url: $$url
}, Symbol.toStringTag, { value: 'Module' }));

export { index$61 as $, index$6s as A, index$6r as B, index$6q as C, index$6p as D, index$6o as E, index$6n as F, index$6m as G, index$6l as H, index$6k as I, index$6j as J, index$6i as K, index$6h as L, index$6g as M, index$6f as N, index$6e as O, index$6d as P, index$6c as Q, index$6b as R, index$6a as S, index$69 as T, index$68 as U, index$67 as V, index$66 as W, index$65 as X, index$64 as Y, index$63 as Z, index$62 as _, index$6R as a, index$51 as a$, index$60 as a0, index$5$ as a1, index$5_ as a2, index$5Z as a3, index$5Y as a4, index$5X as a5, index$5W as a6, index$5V as a7, index$5U as a8, index$5T as a9, index$5s as aA, index$5r as aB, index$5q as aC, index$5p as aD, index$5o as aE, index$5n as aF, index$5m as aG, index$5l as aH, index$5k as aI, index$5j as aJ, index$5i as aK, index$5h as aL, index$5g as aM, index$5f as aN, index$5e as aO, index$5d as aP, index$5c as aQ, index$5b as aR, index$5a as aS, index$59 as aT, index$58 as aU, index$57 as aV, index$56 as aW, index$55 as aX, index$54 as aY, index$53 as aZ, index$52 as a_, index$5S as aa, index$5R as ab, index$5Q as ac, index$5P as ad, index$5O as ae, index$5N as af, index$5M as ag, index$5L as ah, index$5K as ai, index$5J as aj, index$5I as ak, index$5H as al, index$5G as am, index$5F as an, index$5E as ao, index$5D as ap, index$5C as aq, index$5B as ar, index$5A as as, index$5z as at, index$5y as au, index$5x as av, index$5w as aw, index$5v as ax, index$5u as ay, index$5t as az, index$6Q as b, index$41 as b$, index$50 as b0, index$4$ as b1, index$4_ as b2, index$4Z as b3, index$4Y as b4, index$4X as b5, index$4W as b6, index$4V as b7, index$4U as b8, index$4T as b9, index$4s as bA, index$4r as bB, index$4q as bC, index$4p as bD, index$4o as bE, index$4n as bF, index$4m as bG, index$4l as bH, index$4k as bI, index$4j as bJ, index$4i as bK, index$4h as bL, index$4g as bM, index$4f as bN, index$4e as bO, index$4d as bP, index$4c as bQ, index$4b as bR, index$4a as bS, index$49 as bT, index$48 as bU, index$47 as bV, index$46 as bW, index$45 as bX, index$44 as bY, index$43 as bZ, index$42 as b_, index$4S as ba, index$4R as bb, index$4Q as bc, index$4P as bd, index$4O as be, index$4N as bf, index$4M as bg, index$4L as bh, index$4K as bi, index$4J as bj, index$4I as bk, index$4H as bl, index$4G as bm, index$4F as bn, index$4E as bo, index$4D as bp, index$4C as bq, index$4B as br, index$4A as bs, index$4z as bt, index$4y as bu, index$4x as bv, index$4w as bw, index$4v as bx, index$4u as by, index$4t as bz, index$6P as c, index$31 as c$, index$40 as c0, index$3$ as c1, index$3_ as c2, index$3Z as c3, index$3Y as c4, index$3X as c5, index$3W as c6, index$3V as c7, index$3U as c8, index$3T as c9, index$3s as cA, index$3r as cB, index$3q as cC, index$3p as cD, index$3o as cE, index$3n as cF, index$3m as cG, index$3l as cH, index$3k as cI, index$3j as cJ, index$3i as cK, index$3h as cL, index$3g as cM, index$3f as cN, index$3e as cO, index$3d as cP, index$3c as cQ, index$3b as cR, index$3a as cS, index$39 as cT, index$38 as cU, index$37 as cV, index$36 as cW, index$35 as cX, index$34 as cY, index$33 as cZ, index$32 as c_, index$3S as ca, index$3R as cb, index$3Q as cc, index$3P as cd, index$3O as ce, index$3N as cf, index$3M as cg, index$3L as ch, index$3K as ci, index$3J as cj, index$3I as ck, index$3H as cl, index$3G as cm, index$3F as cn, index$3E as co, index$3D as cp, index$3C as cq, index$3B as cr, index$3A as cs, index$3z as ct, index$3y as cu, index$3x as cv, index$3w as cw, index$3v as cx, index$3u as cy, index$3t as cz, index$6O as d, index$22 as d$, index$30 as d0, index$2$ as d1, index$2_ as d2, index$2Z as d3, index$2Y as d4, index$2X as d5, index$2W as d6, index$2V as d7, index$2U as d8, index$2T as d9, index$2t as dA, index$2s as dB, index$2r as dC, index$2q as dD, index$2p as dE, index$2o as dF, index$2n as dG, index$2m as dH, index$2l as dI, index$2k as dJ, index$2j as dK, index$2i as dL, index$2h as dM, index$2g as dN, index$2f as dO, index$2e as dP, index$2d as dQ, index$2c as dR, index$2b as dS, index$2a as dT, index$29 as dU, index$28 as dV, index$27 as dW, index$26 as dX, index$25 as dY, index$24 as dZ, index$23 as d_, index$2S as da, index$2R as db, index$2Q as dc, index$2P as dd, index$2O as de, index$2N as df, index$2M as dg, index$2L as dh, index$2K as di, index$2J as dj, index$2I as dk, index$2H as dl, index$2G as dm, index$2F as dn, index$2E as dp, index$2D as dq, index$2C as dr, index$2B as ds, index$2A as dt, index$2z as du, index$2y as dv, index$2x as dw, index$2w as dx, index$2v as dy, index$2u as dz, index$6N as e, index$12 as e$, index$21 as e0, index$20 as e1, index$1$ as e2, index$1_ as e3, index$1Z as e4, index$1Y as e5, index$1X as e6, index$1W as e7, index$1V as e8, index$1U as e9, index$1t as eA, index$1s as eB, index$1r as eC, index$1q as eD, index$1p as eE, index$1o as eF, index$1n as eG, index$1m as eH, index$1l as eI, index$1k as eJ, index$1j as eK, index$1i as eL, index$1h as eM, index$1g as eN, index$1f as eO, index$1e as eP, index$1d as eQ, index$1c as eR, index$1b as eS, index$1a as eT, index$19 as eU, index$18 as eV, index$17 as eW, index$16 as eX, index$15 as eY, index$14 as eZ, index$13 as e_, index$1T as ea, index$1S as eb, index$1R as ec, index$1Q as ed, index$1P as ee, index$1O as ef, index$1N as eg, index$1M as eh, index$1L as ei, index$1K as ej, index$1J as ek, index$1I as el, index$1H as em, index$1G as en, index$1F as eo, index$1E as ep, index$1D as eq, index$1C as er, index$1B as es, index$1A as et, index$1z as eu, index$1y as ev, index$1x as ew, index$1w as ex, index$1v as ey, index$1u as ez, index$6M as f, index$2 as f$, index$11 as f0, index$10 as f1, index$$ as f2, index$_ as f3, index$Z as f4, index$Y as f5, index$X as f6, index$W as f7, index$V as f8, index$U as f9, index$t as fA, index$s as fB, index$r as fC, index$q as fD, index$p as fE, index$o as fF, index$n as fG, index$m as fH, index$l as fI, index$k as fJ, index$j as fK, index$i as fL, index$h as fM, index$g as fN, index$f as fO, index$e as fP, index$d as fQ, index$c as fR, index$b as fS, index$a as fT, index$9 as fU, index$8 as fV, index$7 as fW, index$6 as fX, index$5 as fY, index$4 as fZ, index$3 as f_, index$T as fa, index$S as fb, index$R as fc, index$Q as fd, index$P as fe, index$O as ff, index$N as fg, index$M as fh, index$L as fi, index$K as fj, index$J as fk, index$I as fl, index$H as fm, index$G as fn, index$F as fo, index$E as fp, index$D as fq, index$C as fr, index$B as fs, index$A as ft, index$z as fu, index$y as fv, index$x as fw, index$w as fx, index$v as fy, index$u as fz, index$6L as g, index$1 as g0, index as g1, index$6K as h, index$6S as i, index$6J as j, index$6I as k, index$6H as l, index$6G as m, index$6F as n, index$6E as o, index$6D as p, index$6C as q, index$6B as r, index$6A as s, index$6z as t, index$6y as u, index$6x as v, index$6w as w, index$6v as x, index$6u as y, index$6t as z };
